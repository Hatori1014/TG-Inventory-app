# Estado del proyecto — leer esto primero

> Última actualización (2026-08-17): **MVP1 completo y cerrado de punta a punta** (HU-06→07→08→09→10, PRs #70/#72/#74/#76/#78, todos mergeados a `staging`). **Incidente de producción encontrado y resuelto el mismo día**: `main` estaba congelado desde antes de HU-01 (el commit real desplegado en Render solo tenía `HealthModule`, sin `AuthModule` ni nada de negocio) — el login daba `404` en producción. Causa: Render solo hace auto-deploy de `main`, y el único intento de sincronizarlo con `staging` (PR #64) había fallado el build silenciosamente (faltaba `prisma generate`). Corregido con **PR #79** (`postinstall: prisma generate` en `backend/package.json`) + **sincronización real `staging` → `main`** (rama `sync/staging-to-main-mvp1`, sin conflictos, verificada local antes de pushear: backend 58 suites/231 tests/lint en verde, frontend build en verde). Verificado en producción tras el merge: `POST /auth/login` → `401` con password incorrecta (ya no `404`), usuario confirmó login real contra el dashboard. Detalle completo en **ADR-30**. **Pendiente, no resuelto**: la preocupación de fondo del usuario sobre UAT — staging y producción siguen compartiendo el mismo backend de Render y la misma Neon DB (`apiUrl` hardcodeado igual en `environment.ts`/`environment.staging.ts`); falta decidir una estrategia real de aislamiento antes de cualquier certificación UAT. **Además, durante estas pruebas de UAT surgieron y se cerraron tres mejoras**: (1) shell de navegación real (`mat-sidenav`/`mat-toolbar`, antes no existía ninguna, había que escribir URLs a mano) + login rediseñado con Angular Material; (2) CORS ajustado para aceptar previews de Vercel además de producción (**ADR-31**), antes solo un origen fijo pasaba, rompiendo el login en cualquier preview; (3) `POST /auth/logout` real con invalidación de token vía blocklist de `jti` (**ADR-32**), a pedido explícito del usuario — revierte la decisión "solo cliente" de ADR-24. Todo verificado local (build/lint/tests en verde en ambos proyectos, más verificación manual real contra Postgres para el logout: login → logout → reusar el mismo token → `401`). **Falta**: mergear estos PRs y correr `npm run prisma:migrate:staging` en Neon para la migración `add_revoked_token` (permiso nuevo no requerido, pero sí migración de schema).

> **TT-24 (tarjeta nueva en Trello, dentro de MVP1) — implementar el diseño de Claude Design, fase 0 en curso.** El usuario exportó el mockup a `docs/Design/` (`TG Inventory UI.dc.html` — 11 pantallas + shell + estados responsive, generado a partir de `ui-ux-design-brief.md`). Es en la práctica un rediseño visual completo de la app; dividido en fases para respetar el WIP limit 1-2. **Fase 0 (tema + shell + login) implementada**: tema real de Angular Material con la paleta exacta del brief (`mat.m2-define-palette` sobre variables CSS en `styles.scss`, ya no el `indigo-pink` genérico), iconos Phosphor (`@phosphor-icons/web`, reemplaza Material Icons — antes solo el shell los usaba), shell y login rediseñados pixel a pixel contra el mockup (gradientes, tipografía, spacing exactos). El login pasó de usar `mat-card`/`mat-form-field` a inputs planos estilizados a mano, igual que el mockup — decisión deliberada: el mockup es ahora la fuente visual autoritativa, no la guía genérica de "usar los patrones de Material" del brief original. **Verificado**: build/lint/test en verde (40 tests), dev server arrancado sin errores, confirmado que el CSS final del build contiene los tokens de color reales. **No se pudo verificar visualmente en navegador real** (sin esa herramienta en este entorno). **Cierre** (2026-08-17): PR #89 mergeado a `staging`, preview de Vercel (`tg-inventory-app-git-staging-hatori2.vercel.app`) desplegado en `READY` — pendiente que el usuario lo revise visualmente ahí antes de seguir. **Fase 1 (Dashboard) implementada**: pasó de un saludo vacío (`<h1>Bienvenido</h1>`, sin un solo link) a un panel real — banner con gradiente y saludo por nombre de pila, grid de accesos rápidos (icono, label, descripción, ruta) y un panel "sin indicadores todavía" (MVP1 no tiene HU de reporting). El grid se arma con `visibleSections()`, la misma fuente de datos que ahora usa el shell (extraída a `core/layout/nav-items.ts`, antes vivía duplicada dentro de `ShellComponent`) — dashboard y sidenav nunca pueden desincronizarse en qué accesos muestran. También se actualizó `frontend/public/logo-placeholder.png` (el usuario cambió el logo). **Hallazgo real durante el TDD de esta fase**: el helper `fakeJwt()` usado en los specs (`shell.component.spec.ts`, `auth.service.spec.ts`, ahora también `dashboard.component.spec.ts`) no codificaba el payload en UTF-8 antes de convertirlo a base64 — con un nombre con tilde ("María Cabrera") el decoder real (`decodeJwtPayload`) tiraba una excepción silenciosa y `AuthService` trataba la sesión como inexistente, dando falsos negativos en el test. No es un bug de producción (el `JwtService` real de NestJS sí codifica UTF-8 correctamente) — corregido solo en el helper de `dashboard.component.spec.ts`; los otros dos archivos no lo necesitaban porque nunca probaron un nombre con tildes, pero tienen la misma fragilidad latente si alguien lo hace. **Verificado**: build/lint/test en verde (43 tests, 3 nuevos), dev server arrancado sin errores. **No se pudo verificar visualmente en navegador real.** **Fix post-Fase 1 (logo/favicon)**: el usuario reportó el logo roto tras mergear la Fase 1. Causa real, no de caché ni de nombre de archivo: **`frontend/public/` nunca estuvo conectado al build** — `angular.json` solo copiaba `src/favicon.ico` (que ni siquiera existe) y `src/assets`; cualquier archivo puesto en `public/` (incluido el `logo-placeholder.png` original) nunca llegó al bundle final, en ningún momento del proyecto. Corregido agregando `{ "glob": "**/*", "input": "public" }` a `assets` en `build` y `test`. De paso: el usuario subió el logo real (`Logo.png`) y un favicon (`faviconTG.png`), ambos movidos a `public/images/` (favicon incluido — el fallback automático de `/favicon.ico` del navegador no aplica a un nombre custom, así que no había motivo real para dejarlo en la raíz), y el logo ahora es un link a `https://tgconsultores.net/` (pestaña nueva) tanto en el shell como en el login. **Verificado**: build/lint/test en verde (43 tests), confirmado con `curl` contra el dev server que `/images/Logo.png` y `/images/faviconTG.png` responden `200` con el tamaño correcto. **Fase 2 (Stock actual) implementada**: rediseño visual completo (filtros, tabla, columna "Lote", paginación) contra la paleta/tipografía del mockup, más un botón "Limpiar filtros" que no existía. **Funcionalidad real agregada, no solo visual**: paginación de verdad — el backend de HU-10 ya soportaba `page`/`pageSize`, pero el frontend nunca los exponía (siempre pedía página 1 con tamaño 100 fijo); ahora hay selector de filas por página (20/50/100, default 20 igual que `PaginationQueryDto`) y botones anterior/siguiente que respetan los límites reales (`total`/`totalPages`). Cambiar cualquier filtro o el tamaño de página vuelve a la página 1. Se quitó el link suelto "Volver" (ya cubierto por el shell). **Verificado**: build/lint/test en verde (50 tests, 7 nuevos cubriendo el cálculo de `totalPages`, los límites de paginación, y que cambiar filtros/tamaño de página reinicia a la página 1), dev server sirve `/inventory/stock` sin errores. **No se pudo verificar visualmente en navegador real.** **Fase 3 (Movimientos) implementada**: selector de tipo como pills con icono (antes un `<select>` plano) y grilla de campos reorganizada contra el mockup — bloque de "Ubicación de destino" (traslado) y "Dirección del ajuste" (ajuste) resaltados en azul cuando aplican. **Panel lateral nuevo** ("Qué se escribe"): muestra en vivo el tipo enviado, cuántas filas escribe el movimiento en el libro mayor ("1" o "2 (salida y entrada)" para traslado) y el delta real de stock en el origen con signo y color (verde/rojo) — a diferencia del mockup, que mostraba texto estático de ejemplo ("+24 o −24 según dirección"), acá el valor sale del estado real del formulario (tipo + dirección + cantidad), porque es un formulario funcional, no una maqueta. Los links sueltos "Gestionar lotes"/"Consultar stock actual" que antes estaban arriba del formulario se movieron al panel lateral como "Atajos" (con iconos), tal como pedía la nota del mockup. **Verificado**: build/lint/test en verde (55 tests, 5 nuevos cubriendo el delta por tipo/dirección, el cambio de tipo vía `selectType()`, y el label del botón de envío), dev server sirve `/inventory` sin errores. **No se pudo verificar visualmente en navegador real.** **Fase 4 (Lotes) implementada**: filtro de producto reorganizado con nota contextual, tarjeta de tabla con header + contador, fila de alta inline al pie de la tabla — todo contra la paleta/tipografía del mockup. **Dos funcionalidades reales agregadas, no solo visuales**: (1) input "Recibido" para antedatar la fecha de recepción de un lote — `CreateBatchRequest.receivedAt` ya existía en el modelo del frontend, pero ningún input lo exponía; (2) columna "Stock del lote" — no hay un endpoint que devuelva totales por lote, así que se calcula en el cliente: al elegir un producto, además de `listBatches()` se pide `GET /inventory/stock` filtrado por ese producto (ya existía desde HU-10) y se suman las cantidades agrupando por `batchId` con `forkJoin`. **Verificado**: build/lint/test en verde (58 tests, 3 nuevos cubriendo el filtro de productos que requieren lote, la suma de stock por lote agrupando múltiples ubicaciones, y que `receivedAt` viaja en el POST cuando se completa), dev server sirve `/inventory/batches` sin errores. **No se pudo verificar visualmente en navegador real.** **Fase 5 (Productos) implementada**: mantuve la navegación por rutas ya existente (`/products` lista y `/products/new`\|`/products/:id/edit` formulario, en vez del layout lado a lado del mockup) y apliqué el estilo del mockup a cada pantalla por separado. Lista: descripción bajo el nombre, badges de "Lote" y "Estado", paginación real (mismo gap que Stock antes de la fase 2 — el backend ya soportaba `page`/`pageSize`, nunca se expuso). Formulario: campos con labels, unidad/categoría con botón "+ Crear" y caja de alta inline resaltada, checkbox de "Requiere lote" como bloque, estado como toggle de pills (solo en edición). **Dos cosas del mockup deliberadamente no replicadas**, documentado en el propio código: (1) el buscador por nombre — `GET /products` no tiene filtro de nombre en el backend, y un buscador que solo filtrara la página cargada sería engañoso, no útil; (2) el botón "+ Nuevo producto"/"Editar" condicionados a permisos (`canWriteProducts` en el mockup) — el frontend no rastrea permisos granulares hoy, solo rol, así que quedan visibles para cualquier autenticado y el backend sigue siendo quien de verdad bloquea con `403` (comportamiento sin cambios). **Verificado**: build/lint/test en verde (62 tests, 4 nuevos cubriendo `totalPages` y los límites de paginación de productos), dev server sirve `/products` y `/products/new` sin errores. **No se pudo verificar visualmente en navegador real.** **Cierre**: PR #97 mergeado a `staging` (confirmado vía API de GitHub, `merged:true`). **Fase 6 (Categorías/Unidades) implementada**: ambas pantallas pasaron de una tabla sin estilo (`<h1>` + form inline + `<table>` plana) al patrón de "catálogo" del mockup — tarjeta con header y contador (`"N categorías"`/`"N unidades"`), columnas Nombre/Productos/Estado/acción, fila de alta inline al pie de la tabla (en vez de un form suelto arriba) y nota de pie "Desactivar no borra...". **Gap real resuelto, no solo visual**: la columna "Productos" del mockup no tiene respaldo en el backend — ni `Category` ni `Unit` exponen un conteo, y no existe endpoint que lo calcule — así que se computa en el cliente: junto al propio catálogo se pide `GET /products` (hasta 100, el volumen actual del proyecto) vía `forkJoin` y se cuenta por `categoryId`/`unitId`, mismo patrón que la Fase 4 usó para el stock por lote. **Verificado**: build/lint/test en verde (68 tests, 6 nuevos — 3 por componente cubriendo el conteo de productos, el alta con `trim()` y el toggle de estado), `ng lint` sin hallazgos, dev server no verificado visualmente en navegador real. **Cierre**: PR #98 mergeado a `staging` (confirmado por commit `b379e6c` en el historial remoto). **Fase 7 (Ubicaciones) implementada**: la pantalla pasó de un `<h1>` + form inline sin estilo a la tarjeta de catálogo del mockup, con dos diferencias reales respecto al patrón de Categorías/Unidades porque el propio mockup las modela distinto: (1) el alta usa un **panel lateral** (grid `1fr 360px`), no una fila inline al pie — hace falta un `<select>` de ubicación padre además del nombre; (2) la tabla no es plana — es una **jerarquía indentada**, calculada en el cliente recorriendo `parentId` con un recorrido en profundidad (`buildHierarchy()`, no hay endpoint de árbol) y `padding-left` proporcional al nivel. **Funcionalidad real agregada, no solo visual**: botón "Renombrar" por fila (edición inline del nombre) — el mockup lo pedía y `PATCH /locations/:id` ya soportaba `name`, pero el componente anterior no lo exponía, solo activar/desactivar. **Una cosa del mockup deliberadamente no replicada**, documentada en el propio código: el ícono por "tipo" de ubicación (edificio/bodega/estante) — `Location` no tiene campo de tipo en el schema, así que todas las filas usan el mismo ícono genérico (`ph-map-pin-area`) en vez de inferir un tipo a partir del nombre. **Verificado**: build/lint/test en verde (75 tests, 7 nuevos cubriendo el orden/indentación de la jerarquía, `parentName()`, alta con `trim()`, toggle de estado, y renombrar incluyendo el caso de nombre sin cambios), `ng lint` sin hallazgos, `ng build` sin errores. **No se pudo verificar visualmente en navegador real.** **Cierre**: PR #99 mergeado a `staging` (confirmado vía API de GitHub, `mergedAt` presente). **Fase 8 (Usuarios) implementada**: la lista pasó de una tabla sin estilo a la tarjeta de catálogo del mockup — avatar circular con iniciales, badge de rol (info) y de estado (Activo/Bloqueado), más paginación real (mismo gap que Products/Stock antes de sus fases: `GET /users` ya soportaba `page`/`pageSize`, nunca se expuso más allá del default de 20). El formulario de alta/edición se rediseñó con el mismo patrón de campos que `product-form` (labels, toggle de estado segmentado, solo en edición) más el aviso del mockup "La edición no cambia la contraseña: todavía no hay flujo de restablecimiento." **Una decisión de arquitectura deliberada, documentada en el propio código**: se mantuvieron las rutas separadas ya existentes (`/users` lista, `/users/new`\|`/users/:id/edit` formulario) en vez de colapsar todo a un panel único con pills "Crear"/"Editar" como propone el mockup — mismo criterio que la Fase 5 usó para Productos: es el cambio más chico sobre un flujo que ya funcionaba, y el formulario (password, rol, estado) ya justifica una pantalla propia. **Dos cosas del mockup deliberadamente no replicadas**, documentado en el propio código: (1) el buscador "Filtrar por nombre o correo" — `PaginationQueryDto` no tiene filtro de texto, mismo criterio que el buscador de Productos en la Fase 5; (2) nada nuevo en permisos — `/users` ya estaba completamente gateada por rol a nivel de router (`roleGuard`), así que no hizo falta lógica de permisos condicional dentro del componente. **Verificado**: build/lint/test en verde (86 tests, 11 nuevos — paginación e iniciales de avatar en la lista, validación de contraseña obligatoria solo en alta, POST/PATCH con el payload correcto, y el mapeo de `409` a "ya existe un usuario con ese correo" en el formulario), `ng lint` sin hallazgos, `ng build` sin errores. **No se pudo verificar visualmente en navegador real.** **Cierre**: PR #100 mergeado a `staging` (confirmado vía API de GitHub, `mergedAt` presente). **Fase 9 (Roles y permisos) implementada — ÚLTIMA FASE DE TT-24**. Lista de roles: pasó de tabla sin estilo a tarjeta de catálogo, con columna "Usuarios" nueva — sin respaldo en el backend (`Role` no tiene conteo propio), calculada en el cliente igual que la Fase 6 hizo con productos por categoría: `forkJoin` con `GET /users` (hasta 100) contando por `roleId`. Alta de rol: card standalone con el mismo patrón de campos que `role-form`/`user-form`. **El cambio más grande de la fase**: `role-permissions` pasó de una `<ul>` plana de checkboxes `module:action` a la **matriz module × action** del mockup — filas por módulo, columnas por acción, con celda "—" cuando el catálogo no tiene esa combinación (ej. `products`/`categories`/`units` no tienen permiso `read`, porque su `GET` está abierto a cualquier autenticado, no por omisión). Las columnas de acción se derivan dinámicamente de las acciones distintas presentes en `GET /permissions` (orden canónico read/create/update/delete, extra alfabético) en vez de hardcodear "solo 3 columnas", así la pantalla no se rompe si el catálogo crece. **Una decisión de arquitectura deliberada, documentada en el propio código**: se mantuvieron las 3 rutas separadas ya existentes (`/roles`, `/roles/new`, `/roles/:id/permissions`) en vez del panel único que propone el mockup — mismo criterio que las Fases 5 y 8 (Productos, Usuarios). **Verificado**: build/lint/test en verde (96 tests, 10 nuevos — conteo de usuarios por rol, alta de rol con description opcional y mapeo de `409`, y la matriz de permisos: derivación de columnas de acción, celda nula cuando el catálogo no tiene la combinación, selección inicial desde los permisos actuales del rol, toggle, y el PATCH con el conjunto completo reemplazado), `ng lint` sin hallazgos, `ng build` sin errores. **No se pudo verificar visualmente en navegador real.** **TT-24 completo**: las 10 fases (0 a 9, todas las pantallas del mockup) están implementadas y mergeadas a `staging`. Falta que el usuario revise el resultado visualmente en el preview de Vercel antes de dar la tarjeta por cerrada en Trello.

> **Post-cierre TT-24 — primera revisión visual real del usuario en el preview de Vercel (2026-08-18)**: encontró que el espaciado entre secciones del sidenav (CATÁLOGO/INVENTARIO/ADMINISTRACIÓN) se veía muy apretado. Primer intento (PR #102, mergeado a `staging`) resultó ser un no-op: el `gap` de flexbox nunca llegaba a aplicarse porque `MatDrawer` envuelve el contenido en su propio `mat-drawer-inner-container`, dos niveles más adentro de donde vivía `.shell-sidenav`. **Fix real** (rama `fix/shell-nav-section-spacing-v2`): `margin-top: 24px` en `.shell-nav-section-label`, confirmado en el CSS compilado y verificado visualmente por el usuario tras limpiar caché. En la misma sesión, el usuario también reportó errores del panel de Problemas de VSCode (la extensión de ESLint marcaba todos los `.ts` con error de parseo) — causa real: `parserOptions.project` en ambos `.eslintrc.js` se resolvía relativo al cwd del proceso, que para la extensión de VSCode es la raíz del repo, no `backend/`/`frontend/` donde vive cada config. Corregido agregando `tsconfigRootDir: __dirname` a ambos — confirmado por el usuario que el panel quedó limpio. **Verificado**: `npm run lint` en verde en ambos proyectos (backend 0 errores/47 warnings preexistentes sin relación, frontend sin hallazgos). **Cierre**: PR #103 mergeado a `staging` (2026-08-19T01:54:53Z) — con esto TT-24 queda completamente cerrada, incluida la corrección de la revisión visual del usuario.

> **Sync `staging` → `main` (2026-08-19)**: como `main` había quedado atrás desde el último sync (PR #80/#83) — le faltaban las 10 fases de TT-24, el logout real (ADR-32/TT-27) y ambos fixes de PR #103 —, se abrió y mergeó **PR #104** (`sync/staging-to-main-tt24-shell-fixes`), mismo patrón de ADR-30: rama dedicada desde `main`, merge de `staging` adentro. Único conflicto real: `docs/esp/decisiones.md`/`docs/en/decisions.en.md` (`main` ya tenía ADR-31 backporteado vía PR #86 pero no ADR-32) — resuelto conservando la entrada de ADR-32 de `staging`, sin tocar código. Verificado local antes de commitear el merge: backend `lint`/`build` en verde, 60 suites/240 tests; frontend `lint`/`build` en verde, 96/96 tests.

> **TT-27 — Logout real (ADR-32) — ✅ Cerrada.** El código ya estaba mergeado a `staging` antes de esta sesión (confirmado: el logout llegó como parte del merge de `staging` hacia el PR #104 de sync); lo que faltaba era correr `npm run prisma:migrate:staging` para la migración `20260817050000_add_revoked_token` — **nunca se había ejecutado**, pese a estar marcada como pendiente en este mismo archivo y en la propia tarjeta de Trello. **Incidente real encontrado en producción (2026-08-19)**: el usuario reportó `POST /roles` → `500` al probar la app recién sincronizada a `main`. Diagnóstico vía logs reales de Render (`list_logs`, MCP): `PrismaClientKnownRequestError: The table "public.revoked_token" does not exist`, lanzado desde `JwtStrategy.validate()` — como **producción y staging comparten la misma Neon DB y el mismo servicio Render** (ver ADR-30), esto rompía **todo endpoint autenticado**, no solo `/roles`, para cualquier usuario cuyo JWT ya llevara `jti` (cualquier login posterior al merge del logout). Corregido corriendo `npm run prisma:migrate:staging` contra la DB real — aplicó dos migraciones pendientes: `20260815000000_add_location_name_unique_per_parent` (de HU-06, tampoco se había corrido) y `20260817050000_add_revoked_token`. **Verificado**: `/health` → `200 OK` antes y después; logs de Render sin errores nuevos desde la migración (`list_logs` filtrado por `level:error` desde antes del fix hasta ~1h después, vacío). **Pendiente de confirmación del usuario**: reintentar la acción que dio `500` (crear un rol) en el navegador para cerrar el loop — el usuario pasó a instrucciones de MVP2 antes de confirmarlo explícitamente en esta sesión.

Actualízalo tú mismo al cerrar cada iteración. English version below.

## Nota de estructura

`docs/` está organizado en subcarpetas — `docs/esp/` (español), `docs/en/` (inglés), `docs/Diagrams/` (todos los PNG). Ver ADR-16 en `decisiones.md`.

## Qué es este proyecto

Sistema de control de inventario: proveedores, inventario por ubicaciones, alertas de stock, historial de compras, comparativa de precios, RBAC, y solicitudes internas con flujo de aprobación. Un solo desarrollador, fines de semana. Ver `docs/esp/PRD.md` para el detalle completo.

## Dónde estamos ahora mismo

**Fase**: Iteración 1 cerrada (HU-01→02→03→28). **Iteración 2 (HU-06→07→08→09→10) cerrada de punta a punta** — HU-06, HU-07, HU-08, HU-09 y HU-10 todas mergeadas a `staging` y verificadas (PR #70, #72, #74, #76, #78). **`main` sincronizado con `staging`** (2026-08-17 vía ADR-30, y de nuevo 2026-08-19 vía PR #104 — TT-24 completa + logout real/TT-27). **MVP1 y su ronda de UAT quedan completamente cerrados** con el cierre de TT-24 y TT-27 (ver notas arriba). **Arranca MVP2** (2026-08-19): HU-04 → HU-13 → HU-05 → HU-14, orden acordado con el usuario (HU-04 es la base — proveedor real antes de poder comprarle; HU-13 depende de HU-04 y reusa el motor de movimientos de HU-07/08; HU-05 depende de HU-13 para tener compras reales que listar; HU-14 depende de HU-13 para tener precios reales de comparar, y cierra MVP2).


### Próximo paso inmediato: **HU-04 y HU-13 completas y cerradas** (PR #106/#107/#108/#109 mergeados a `staging`, migraciones y seed corridos contra Neon — reverificado independientemente con `prisma migrate status`, 10/10 sin drift). **HU-05 implementada** (rama `feature/hu-05-supplier-purchase-history`, ver tabla abajo), PR pendiente de abrir. Sigue **HU-14 — Comparativa de precios de compra**, la última de MVP2; DoR ya resuelto por el usuario (dos vistas tabla: precio de un producto entre proveedores, y promedio mensual entre hasta 3 proveedores). TT-28 (estrategia de aislamiento staging/producción) sigue pendiente pero no bloquea el desarrollo — es una decisión de negocio/infraestructura que el usuario resolverá aparte; solo bloquea certificar una UAT real de MVP2 al final, no el código. Ver ADR-30 y la nota de memoria persistida sobre este tema.

TT-02 a TT-05 ya están cerradas (ver tabla abajo). Las nueve tareas técnicas de la revisión de arquitectura (ver "Gaps de arquitectura" abajo) están todas cerradas — mergeadas a `staging` y, donde aplica (TT-17/TT-18), con su migración ya aplicada en Neon staging.

**TT-23 — Catálogos parametrizables (Category, Unit)** — ✅ Cerrada (2026-08-14), mergeada a `staging` (PR #61). A pedido del usuario, se validó si el schema permite que un rol administre "tablas catálogo" desde el front. Hallazgo: `Role`/`Permission` (HU-02) y `ApprovalFlow` (ADR-08) ya eran tablas reales parametrizables; `Product.category`/`Product.unit` eran `String` libre sin tabla propia. Solución: `Category`/`Unit` agregadas como tablas (mismo patrón que `Role`), `Product.categoryId`/`unitId` como FK — ver **ADR-23**. Migración `20260814045501_add_category_unit_catalogs` aplicada en local y también en Neon staging (`npm run prisma:migrate:staging`) — verificado con `curl` al backend real en Render tras la migración: `/health` → `200 OK`, `database: ok`. Endpoints (`/categories`, `/units`) documentados en el plan; implementación de controllers/services diferida a cuando se implemente HU-28, cuyo texto ya se ajustó para reflejar el nuevo diseño (selects contra catálogo, no texto libre).

El flujo de PR + CI **funciona y ya se probó de punta a punta** (ver detalle de TT-07 abajo), y ahora además está reforzado por la regla de branch protection.

### Gaps de arquitectura (TT-14 a TT-21)

Origen: sesión de revisión de arquitectura (2026-08-12), motivada por una pregunta concreta — "si falla un módulo, ¿se caen otros?". El diseño es monolito modular (todos los módulos en un solo proceso Node, un solo `schema.prisma`/`PrismaClient`) — sin disciplina explícita, la respuesta es sí. Ocho TT (no HU, son prerrequisitos técnicos transversales) atacan esto y gaps relacionados (concurrencia, idempotencia, paginación, índices, logging, fronteras entre módulos), antes de implementar cualquier HU de negocio. Detalle completo de cada una en Trello, lista "Iteración actual".

- **TT-14 (fronteras entre módulos)** — ✅ Hecho. Ver fila en la tabla de abajo.
- **TT-15 (manejo de errores global)** — ✅ Hecho. Ver fila en la tabla de abajo.
- **TT-16 (límites de conexión Prisma)** — ✅ Hecho. Ver fila en la tabla de abajo.
- **TT-21 (logging)** — ✅ Hecho. Ver fila en la tabla de abajo.
- **TT-19 (paginación)** — ✅ Hecho. Ver fila en la tabla de abajo.
- **TT-20 (índices)** — ✅ Hecho. Ver fila en la tabla de abajo.
- **TT-17 (locking optimista en stock)** — ✅ Hecho. Ver fila en la tabla de abajo.
- **TT-18 (idempotencia)** — ✅ Hecho. Ver fila en la tabla de abajo.

**TT-22 — Borrado lógico** (nueva, no es parte de las ocho originales): hallazgo durante TT-20 — no hay `DELETE` planeado en ningún endpoint de la API, y 13 de 17 modelos no tienen ni `status` ni `deletedAt`. Decisión tomada y documentada como **ADR-22**: `deletedAt DateTime?` se agrega modelo por modelo, solo cuando la primera HU real defina una acción de "eliminar" sobre ese modelo — nunca de forma preventiva en los 17 a la vez — con lectura filtrada vía extensión de Prisma Client. No se toca ningún modelo todavía (no hay HU que lo consuma) — la decisión en sí ya está mergeada a `staging`.

### Tareas técnicas, en detalle

| Tarea técnica | Estado |
|---|---|
| TT-01 (crear repo en GitHub) | ✅ Hecho — push confirmado |
| TT-11 (health-check) | ✅ Hecho — re-validado |
| TT-12 (Prisma + migración) | ✅ Hecho — re-validado |
| TT-13 (Docker Postgres local) | ✅ Hecho — re-validado |
| TT-07 (CI) | ✅ Hecho — verificado con PRs reales (#13, #14) contra `staging`, ambos checks (`backend`, `frontend`) en verde. Ver "Qué se encontró y arregló" abajo. **Refinamiento (ADR-19)**: los checks tenían `paths:` en el trigger, así que en PRs de solo-docs (varios en esta sesión) el check requerido nunca se disparaba y branch protection quedaba esperando para siempre — obligaba a usar bypass. Se quitó `paths:` del trigger y se agregó `dorny/paths-filter` para gatear el trabajo real por step; ahora un PR de solo-docs pasa en segundos sin bypass, y uno que sí toca `backend/`/`frontend/` corre el pipeline completo igual que antes |
| TT-02 (branching + branch protection) | ✅ Hecho — repo pasado a público; dos rulesets creados en GitHub (`main` y `staging`), Enforcement status `Active` en ambos, con `Require status checks to pass` exigiendo `backend` y `frontend`. Falta sincronizar el estado en Trello (sin conector desde este entorno) |
| TT-03 (Vercel) | ✅ Hecho — proyecto conectado al repo (auto-deploy en push a `main` confirmado); tras corregir Framework Preset (`Angular`) y Output Directory (`dist/frontend/browser`, requerido por el builder `application` de Angular 18), `https://tg-inventory-app.vercel.app` responde `200 OK` sirviendo `index.html` |
| TT-04 (Render/Fly.io) | ✅ Hecho — servicio `tg-inventory-backend` creado en Render (`https://tg-inventory-backend.onrender.com`), conectado al repo; variables `DATABASE_URL`, `JWT_SECRET`, `JWT_EXPIRES_IN`, `NODE_ENV`, `FRONTEND_URL` cargadas. Verificado con `curl`: `/health` → `200 OK` (`database: ok`), y CORS (GET + preflight OPTIONS) devuelve `access-control-allow-origin: https://tg-inventory-app.vercel.app` |
| TT-05 (Neon/Supabase) | ✅ Hecho — base de datos creada en Neon, migraciones aplicadas con `npm run prisma:migrate:staging`, `DATABASE_URL` ya copiada a los secrets de Render (TT-04) |
| TT-06 (Cloudflare) | ✅ Hecho (2026-08-14, 5/5 ítems, confirmado por el usuario) — cuenta creada en cloudflare.com; WAF/proxy en espera (sin dominio propio agregado todavía, se usan los subdominios gratuitos de Vercel/Render — no bloqueante); bucket R2 creado; API token generado con permisos limitados a ese bucket; credenciales (`R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`) guardadas solo localmente por ahora — correcto, R2 no se consume desde ningún código hasta MVP 5 (HU-26/27), se cargan como secret real en Render cuando llegue ese momento |
| TT-08 (CD) | ✅ Hecho (2026-08-14, 6/6 ítems) — **NO es un workflow**, es configuración en los dashboards de Vercel/Render. `apiUrl` real en `environment.prod.ts`/`environment.staging.ts` mergeado (PR #32); Vercel Production Branch = `main` (confirmado por el usuario en el dashboard); Vercel genera preview/branch deploy automático para `staging` y cualquier rama (confirmado con datos reales del historial de deployments, alias `tg-inventory-app-git-staging-*`); Render auto-deploy en "On Commit" apuntando a `main` (confirmado por el usuario); branch protection activo en `main`/`staging` (TT-02). Segundo servicio Render para "staging" **pospuesto deliberadamente** (decisión 2026-08-14) — un solo servicio alcanza mientras no haya separación real staging/producción con HU de negocio |
| TT-09 (Dependabot) | ✅ Hecho (2026-08-14, 4/4 ítems) — `dependabot.yml` agrupa por ecosistema. "Dependabot version updates" confirmado con evidencia real (PR #28 de `dependabot[bot]`); gate de `npm audit` en CI ya verificado en TT-07; "Dependabot alerts" y "Dependabot security updates" activados por el usuario en GitHub → Settings → Code security |
| TT-10 (secrets) | ✅ Hecho, verificado ítem por ítem (2026-08-14) — `DATABASE_URL`, `JWT_SECRET`, `JWT_EXPIRES_IN`, `NODE_ENV`, `FRONTEND_URL` cargados en Render (nunca en el repo, ver `.env.example`). **GitHub Actions no necesita secrets**: `ci-backend.yml`/`ci-frontend.yml` no usan `secrets.*` ni tocan una BD real o la API desplegada (solo `lint`/`test` unitario/`audit`/`build`). **Vercel tampoco necesita secrets hoy**: `apiUrl` es una URL pública hardcodeada, no una credencial. **Verificación de seguridad ejecutada ahora**: `git log --all --full-history -- backend/.env` y `-- backend/.env.staging` devuelven vacío — ambos archivos correctamente en `.gitignore`, nunca se subió un `.env` real. Se revisita si algún día se agrega un secret real a GitHub Actions o Vercel |
| TT-14 (fronteras entre módulos) | ✅ Hecho — ADR-18 documentado; `eslint-plugin-boundaries` en `backend/.eslintrc.js`, falla `npm run lint` (y por tanto `ci-backend.yml`) si un módulo importa `domain/`/`infrastructure/` de otro. Verificado con fixture temporal (mismo módulo pasa, cruzado falla con el mensaje del ADR-18) — fixture descartado después de confirmar |
| TT-15 (manejo de errores global) | ✅ Hecho — `GlobalExceptionFilter` (`backend/src/common/filters/`) registrado vía `APP_FILTER`: cualquier excepción (conocida o no) responde con formato consistente (`statusCode`, `message`, `timestamp`, `path`); errores desconocidos devuelven 500 genérico sin filtrar detalles internos, logueados server-side. `process.on('uncaughtException'/'unhandledRejection')` en `main.ts` loguean y cierran controladamente (Render reinicia el contenedor). Verificado con 3 tests unitarios (`global-exception.filter.spec.ts`) y probado en vivo: servidor compilado levantado localmente, `/health` → `200`, ruta inexistente → `404` con el formato del filtro |
| TT-16 (límites de conexión Prisma) | ✅ Hecho, incluida la variable real en Render — `connection_limit=5`, `pool_timeout=10`, `connect_timeout=10`, `options=-c statement_timeout=10000` documentados en `.env.example`/`TRD.md` y ya aplicados en el `DATABASE_URL` de Render. Verificado empíricamente contra Postgres local (query 2s pasa, 15s se cancela a los ~10s con el error real de Postgres `57014`) y en vivo: `curl` a `/health` en producción → `200 OK`, `database: ok`. Números de Neon (104 conexiones a 0.25 CU, 97 tras reservar 7 para el superusuario) confirmados en su documentación oficial. **Incidente durante el rollout**: el primer intento en Render falló con `P1001 (Can't reach database server)` — la causa real era una comilla `"` sobrante al final (y probablemente al inicio) del valor pegado en el campo de Render, arrastrada del formato `DATABASE_URL="..."` de los archivos `.env` (Render no le hace ese parseo tipo dotenv, toma el valor tal cual se pega). Reproducido localmente para confirmar antes de indicar el fix. **Gotcha a tener en cuenta** al copiar cualquier otro valor desde `.env.example`/`.env.staging` hacia el dashboard de Render: pegar solo el contenido entre comillas, nunca las comillas mismas |
| TT-21 (logging estructurado) | ✅ Hecho — `nestjs-pino` (`backend/src/config/logger.config.ts`, cargado en `app.module.ts`, `app.useLogger()` en `main.ts` con `bufferLogs: true`). JSON estructurado a stdout en `staging`/`production`, pretty-print en `development`. Correlation id por request (`X-Request-Id`, propagado si el cliente ya lo manda). Redacción automática de `Authorization`, cookies, `password`/`token` del body — nunca en texto plano. Convención documentada en `convenciones.md` (qué loguear: errores/5xx siempre, escrituras críticas explícitas, requests ya cubiertos por `pino-http`). Verificado en vivo con el servidor compilado en ambos modos: JSON crudo en `production`, pretty-print en `development`, header `Authorization` con token real confirmado como `[Redacted]` en el log. `npm run lint`/`build`/`test` en verde |
| TT-19 (paginación estándar) | ✅ Hecho — convención offset/limit para todo endpoint de listado futuro (HU-05/08/10), documentada en `TRD.md` sección 4 y `convenciones.md`. Implementación compartida en `backend/src/common/`: `dto/pagination-query.dto.ts` (`page`/`pageSize`, validado con `class-validator`, tope `pageSize` máx. 100 para evitar el "noisy neighbor" de TT-16), `dto/paginated-response.dto.ts` (shape `{ items, total, page, pageSize }`), `utils/pagination.util.ts` (`toPrismaSkipTake()`, `buildPaginatedResponse()`). 8 tests unitarios nuevos (defaults, validación de límites, cálculo de `skip`/`take`). De paso se corrigió un gap real en `jest.config.js` — faltaba `setupFiles: ['reflect-metadata']`, sin eso cualquier DTO con `@Type()` de `class-transformer` fallaba en tests con `Reflect.getMetadata is not a function`. `npm run lint`/`build`/`test` en verde (12 tests, toda la suite) |
| TT-20 (índices de BD) | ✅ Hecho — verificado empíricamente que PostgreSQL no indexa FK automáticamente (a diferencia de MySQL): ninguna de las 27 columnas FK en la migración inicial tenía índice. 17 índices nuevos agregados con criterio documentado en `TRD.md` sección 3 (toda FK usada en filtros/joins, salvo tablas de config muy pequeñas; compuestos donde el patrón de consulta lo es — `InventoryMovement[productId, locationId]`, `Purchase[supplierId, purchasedAt]`, `AuditEvent[entity, entityId]`; sin duplicar lo ya cubierto por `@@unique`). Migración `20260813043614_add_indexes` generada y aplicada en local (`prisma migrate dev`), SQL revisado línea por línea (solo `CREATE INDEX`, nada más). Verificado en vivo: servidor compilado levantado, `/health` → `200 OK`. Aplicada también en Neon staging con `npm run prisma:migrate:staging` (`migrate deploy`, mecanismo de ADR-15) — verificado con `curl` al backend real en Render tras la migración: `/health` → `200 OK`, `database: ok`, sin downtime. **Hallazgo aparte, fuera de alcance**: no hay borrado lógico (`deletedAt`) ni `DELETE` planeado en ningún endpoint de la API — 13 de 17 modelos no tienen ni el patrón `status` que sí usan `User`/`Location`/`Product`/`Supplier`. No se tocó nada al respecto; queda para una TT nueva que el usuario agregará a Trello |
| TT-17 (locking optimista en stock) | ✅ Hecho — mergeado a `staging` (PR #44). Columna `version Int @default(0)` en `LocationStock` (migración `20260813180448_add_location_stock_version`, un solo `ALTER TABLE ADD COLUMN`, revisada línea por línea), utilidad reusable `withOptimisticLock()` (`backend/src/common/utils/optimistic-lock.util.ts`): reintenta hasta 3 veces ante un conflicto de versión, luego lanza `409 Conflict`. Decisión documentada en **ADR-20** (descarta `SELECT FOR UPDATE` y 409 sin reintento). Cubierta con 4 tests unitarios TDD. Sin consumidor real todavía (Iteración 0, no hay use-case de movimientos implementado) — queda lista para HU-08/HU-16/HU-17. Migración aplicada también en Neon staging (`npm run prisma:migrate:staging`) — verificado con `curl` al backend real en Render tras la migración: `/health` → `200 OK`, `database: ok` |
| TT-18 (idempotencia) | ✅ Hecho — mergeado a `staging` (PR #45). Tabla `IdempotencyKey` (`key` único, `endpoint`, `response` `Json`, `createdAt`; migración `20260813181409_add_idempotency_key`, un solo `CREATE TABLE` + índice único), decorador `@Idempotent()` + `IdempotencyInterceptor` reusables (`backend/src/common/`): exige header `Idempotency-Key`, devuelve la respuesta ya guardada sin re-ejecutar el handler si la key se repite, y resuelve la carrera entre dos requests concurrentes con la misma key vía el código `P2002` de Postgres (constraint único), devolviendo la respuesta que ganó en vez de fallar. Decisión documentada en **ADR-21** (descarta Redis, deshabilitar el botón en frontend). Cubierta con 5 tests unitarios (mocks de `PrismaService`/`Reflector`/`ExecutionContext`, incluyendo el caso de carrera). Sin consumidor real todavía; queda lista para HU-08/HU-13/HU-15/HU-16. Migración aplicada también en Neon staging junto con la de TT-17 — verificado con `curl` al backend real en Render: `/health` → `200 OK`, `database: ok` |
| TT-22 (borrado lógico: estrategia y alcance) | ✅ Hecho — mergeado a `staging` (PR #46). **ADR-22**: `deletedAt DateTime?` se agrega modelo por modelo, solo cuando la primera HU real defina una acción de "eliminar" sobre ese modelo sin `status` ya existente (`User`/`Location`/`Product`/`Supplier` no se tocan); lectura filtrada vía extensión de Prisma Client (`$extends`), no `where: { deletedAt: null }` repetido a mano. Nunca aplica a `InventoryMovement`/`AuditEvent` (registro histórico, ADR-07) ni a tablas de enlace/config o filas derivadas sin ciclo de vida propio (`RolePermission`, `ApprovalFlow`, `LocationStock`, `PurchaseItem`, `RequestItem`). Sin cambios de código ni de schema — es explícitamente lo que pide la propia tarjeta de Trello (aplicar el patrón solo cuando aparezca la primera HU que lo necesite, no preventivamente) |

### Historias de usuario (HU), en detalle

| HU | Estado |
|---|---|
| HU-01 (iniciar sesión) | ✅ Mergeada a `staging` (PR #65, rama `feature/hu-01-login` ya borrada local y remota tras el merge). Backend: módulo `auth` nuevo (`backend/src/modules/auth/`, arquitectura hexagonal — `domain/auth-user.entity.ts` con `isActive()`, `application/use-cases/login.use-case.ts`, `infrastructure/user.prisma.repository.ts` + `jwt.strategy.ts`), `POST /auth/login` (único endpoint, `@Public()`), mismo mensaje de error genérico para usuario inexistente/password incorrecto/usuario bloqueado (evita enumeración). `JwtAuthGuard`/`RolesGuard` ahora registrados **globales** vía `APP_GUARD` (antes eran stubs sin conectar); decorador nuevo `@Public()` (`backend/src/common/decorators/public.decorator.ts`) usado en `POST /auth/login` y en `GET /health` (si no, `/health` se hubiera roto para el monitoreo de Render/CD). Decisión completa en **ADR-24**. Seed nuevo (`backend/prisma/seed.ts`, idempotente, requiere `SEED_ADMIN_EMAIL`/`SEED_ADMIN_PASSWORD` en el entorno — nunca genera ni loguea una contraseña), scripts `prisma:seed`/`prisma:seed:staging` agregados. Sin migración de schema nueva (los modelos `User`/`Role` ya existían). Ajustes de tooling necesarios para que esto funcionara: `backend/jest.config.js` ampliado para descubrir `test/bdd/` (antes solo veía `src/`), `backend/tsconfig.build.json` nuevo + `nest-cli.json` apuntando a él (separa el build, que excluye `test/`/`prisma/`, del proyecto usado por ESLint/`tsconfig.json`, que sí necesita verlos) — sin esto `nest build`/`nest start` fallaban con `TS6059` al toparse con archivo fuera de `rootDir`. Frontend: `AuthService` nuevo (`core/services/`, señal `user`, sesión restaurada decodificando el JWT de `localStorage`, sin endpoint `/auth/me`), `jwt.util.ts` (decodifica payload sin instalar `jwt-decode`), `login.component.ts` conectado de verdad (antes `onSubmit()` estaba vacío), `role.guard.ts` corregido — **hallazgo de seguridad real**: antes no verificaba autenticación en absoluto, cualquiera sin login podía entrar a `/suppliers`; ahora exige `isAuthenticated()` primero. Pantalla `/dashboard` placeholder nueva (destino post-login para todos los roles, no hay pantallas propias por rol todavía). **Verificado**: backend — `npm run lint`/`build`/`test` en verde (11 test suites, 35 tests: TDD del use-case/entidad/controller/guard + BDD de 4 escenarios de login vía `jest-cucumber`+`supertest` contra un repositorio fake en memoria); frontend — `npm run lint`/`build`/`ng test --browsers=ChromeHeadless` en verde (12 tests). **Verificación manual real, local** (backend levantado con `start:dev`, seed corrido con credenciales de prueba `admin@tg-group.local`/`Verification-Test-123`): `GET /health` → `200` sin token; `POST /auth/login` con credenciales correctas → `200` + `accessToken` + `user` con rol; con password incorrecto y con usuario inexistente → `401` con el mismo mensaje genérico en ambos casos. Frontend (`ng serve`) compiló y sirvió `/login` con `200`, chunks lazy de `login-component`/`dashboard-component` presentes. **No se pudo verificar visualmente en navegador real** (sin `chromium-cli`/Playwright disponibles en este entorno Windows sin una instalación nueva no solicitada) — la UI se probó por HTTP/build/unit tests, no con captura de pantalla. **Cierre**: PR mergeado a `staging`; verificación en Neon staging/Render con `npm run prisma:seed:staging` queda a cargo del usuario (requiere su propio `SEED_ADMIN_PASSWORD` en `.env.staging`), no se hizo desde este entorno. |
| HU-02 (crear roles y asignar permisos) | ✅ Mergeada a `staging` (PR #66). Backend: nuevo módulo `roles` (`backend/src/modules/roles/`, arquitectura hexagonal — `domain/role.entity.ts` con `hasPermission(module, action)`, `domain/permission.value-object.ts` (Value Object auto-validado), 4 use-cases, `infrastructure/role.prisma.repository.ts` + `permission.prisma.repository.ts`), endpoints `GET/POST /roles`, `PATCH /roles/:id` (reemplaza el set completo de permisos, transaccional), `GET /permissions` (no está en la tabla del plan maestro sección 7.4, agregado para que el frontend arme el formulario de asignación). Guard nuevo **`PermissionsGuard`** (`backend/src/common/guards/permissions.guard.ts`) + decorador `@RequirePermission(module, action)` — distinto de `RolesGuard` (HU-01, compara nombre de rol): este consulta `RolePermission` en cada request marcado, registrado `APP_GUARD` dentro de `roles.module.ts` (mismo patrón que `auth.module.ts`). Decisión completa en **ADR-25**. Migración aditiva `20260814230000_add_role_permission_unique_constraints` (`Role.name` y `Permission[module,action]` ahora `@unique`) — **generada a mano** (`prisma migrate dev` no corre en este entorno de herramientas, no hay TTY interactivo disponible; se escribió el SQL directamente siguiendo el patrón exacto de migraciones previas del proyecto como `add_category_unit_catalogs`, se aplicó con `prisma migrate deploy` y se confirmó sin drift con `prisma migrate status`). Bootstrap del huevo-y-la-gallina: `seed.ts` extendido para sembrar 3 permisos (`roles:read/create/update`) y otorgárselos al rol "Administrador" — **quien ya corrió el seed de HU-01 debe volver a correrlo** (`npm run prisma:seed`), confirmado localmente (el admin sembrado en HU-01 pasó de 0 a 3 permisos tras re-sembrar). Refactor menor de código de HU-01: `isUniqueConstraintViolation()` extraída de `idempotency.interceptor.ts` a `common/utils/prisma-error.util.ts` (reusada por `CreateRoleUseCase`); `FakeAuthUserRepository` del BDD extraída a `test/bdd/support/` (reusada por el BDD nuevo). Frontend: modelos `role.model.ts`/`permission.model.ts`/`paginated-response.model.ts`, `RolesService` (feature-scoped, `features/users-roles/`), 3 componentes (`roles-list`, `role-form`, `role-permissions` con checkboxes), ruta `/roles` protegida por el `roleGuard` existente (`data: { roles: ['Administrador'] }` — sin sistema de permisos granular en el frontend en esta ronda, decisión explícita en ADR-25). **Verificado**: backend — `npm run lint`/`build`/`test` en verde (22 test suites, 72 tests: TDD del domain/use-cases/guard/controllers nuevos + BDD de 3 escenarios en `manage-roles.feature`, incluye el criterio literal de la HU — usuario sin permiso `roles:create` → `403`); frontend — `npm run lint`/`build`/`ng test --browsers=ChromeHeadless` en verde (16 tests). **Verificación manual real, local** (backend levantado con `start:dev`): login admin → `POST /roles` crea rol → `201`; mismo nombre de nuevo → `409`; `GET /roles`/`GET /permissions` listan paginado; `PATCH /roles/:id` asigna permiso → `200`; `PATCH` con `permissionId` inexistente → `400`; `PATCH` sobre rol inexistente → `404`; sin token → `401`; usuario con rol "Comprador" (sin `roles:create`, creado ad hoc para la prueba) intenta crear un rol → **`403`** confirmando el criterio de aceptación exacto de la HU, y el mismo usuario sí puede `GET /roles` (tiene `roles:read`) → `200`. Frontend (`ng serve`, servidor viejo detectado y matado antes de repetir la prueba para evitar falso positivo) compiló limpio con los chunks nuevos (`roles-list-component`, `role-form-component`, `role-permissions-component`, `users-roles-routes`). **No se pudo verificar visualmente en navegador real** (misma limitación de entorno que HU-01, sin `chromium-cli`/Playwright). **Cierre**: PR mergeado a `staging`; migración y seed en Neon staging quedan a cargo del usuario, no se hicieron desde este entorno. |
| HU-03 (asignar rol a usuario) | ✅ Mergeada a `staging` (PR #67). Backend: nuevo módulo `users` (`backend/src/modules/users/`, arquitectura hexagonal — `domain/user.entity.ts` con `isActive()`, 4 use-cases incluyendo `ValidateUserCredentialsUseCase` exportado, `infrastructure/user.prisma.repository.ts`), endpoints `GET/POST /users`, `PATCH /users/:id` (edita name/email/roleId/status — **nunca password**, sin endpoint de reseteo todavía, decisión explícita en ADR-26). **Refactor de `auth`** (resuelve deuda técnica marcada explícitamente por HU-01): `auth` ya no tiene repositorio propio de `User` — se borran `auth/domain/auth-user.entity.ts`(+spec)/`auth-user.repository.interface.ts`/`auth/infrastructure/user.prisma.repository.ts`, `login.use-case.ts` reescrito para consumir `ValidateUserCredentialsUseCase` (exportado por `UsersModule`), que hace `bcrypt.compare`+`isActive()` puertas adentro y nunca deja salir la entidad `User` ni su `passwordHash` — efecto colateral bueno: el mensaje genérico de credenciales inválidas queda estructuralmente garantizado (login ya no puede distinguir las 3 causas). `AuthenticatedUserDto` se mueve de `auth/dto/` a `users/dto/`. Nueva `isForeignKeyViolation()` (P2003) en `common/utils/prisma-error.util.ts`, junto a la `isUniqueConstraintViolation()` (P2002) ya existente de HU-02 — valida que `roleId` exista de verdad sin que `users` consulte el módulo `roles` (ADR-18). Bootstrap: `seed.ts` extendido con `users:read/create/update` otorgados al Administrador — quien ya corrió el seed de HU-02 debe volver a correrlo. Decisión completa en **ADR-26**. Sin migración de schema nueva (el modelo `User` ya existía completo). Migración necesaria de BDD ya existentes: `login.steps.ts` y `manage-roles.steps.ts` (ambos dependían de `AUTH_USER_REPOSITORY`, eliminado) se actualizaron a `USER_REPOSITORY`/`FakeUserRepository` nuevo (`test/bdd/support/`, reemplaza a `fake-auth-user.repository.ts`) — confirmado que `overrideProvider` de `@nestjs/testing` sigue reemplazando el token en todo el árbol de módulos sin importar cuántos lo redeclaren. Frontend: `user-account.model.ts` (nombre distinto de `user.model.ts`, que es el modelo de sesión de HU-01), `UsersService`, rutas separadas en `ROLES_ROUTES`/`USERS_ROUTES` dentro de `users-roles.routes.ts` (antes un solo `USERS_ROLES_ROUTES` con solo roles), `app.routes.ts` con dos entradas (`/roles`, `/users`). Componentes `users-list` y `user-form` (un componente, dos modos: crear pide password, editar no — editar resuelve el usuario filtrando del `listUsers()` paginado, mismo criterio que `role-permissions.component.ts` de HU-02, no hay `GET /users/:id` en el plan maestro). **Verificado**: backend — `npm run lint`/`build`/`test` en verde (28 test suites, 94 tests: TDD del domain/use-cases/controller nuevos + `login.use-case.spec.ts` reescrito + BDD nuevo de 4 escenarios en `manage-users.feature`, incluye el criterio literal de la HU — reasignar rol se refleja en el próximo login); frontend — `npm run lint`/`build`/`ng test --browsers=ChromeHeadless` en verde (19 tests). **Verificación manual real, local** (backend levantado con `start:dev`): login admin sigue funcionando tras el refactor; `POST /users` crea usuario con rol "Comprador" → `201`; email duplicado → `409`; `roleId` inexistente → `400`; sin token → `401`; login del usuario recién creado → refleja rol "Comprador" en el JWT; admin reasigna el usuario a rol "Administrador" vía `PATCH /users/:id` → `200`; `PATCH` sin campos → `400`; **login del mismo usuario de nuevo → el JWT ahora trae rol "Administrador"**, confirmando el criterio de aceptación exacto de la HU en vivo. Frontend (`ng serve`, servidor viejo detectado y matado antes de repetir la prueba) compiló limpio con los chunks nuevos (`users-list-component`, `user-form-component`). **No se pudo verificar visualmente en navegador real** (misma limitación de entorno que HU-01/HU-02, sin `chromium-cli`/Playwright). **Cierre**: PR mergeado a `staging`; seed en Neon staging queda a cargo del usuario, no se hizo desde este entorno. |
| HU-28 (crear y editar productos del catálogo) | ✅ Mergeada a `staging` (PR #69). Última HU de esta tanda, cerrada de punta a punta: seed corrido en Neon staging (`npm run prisma:seed:staging`, otorga `products/categories/units:create/update` al Administrador) y verificado contra el backend real de Render (`/health` → `200 OK`, `database: ok`). Backend: nuevo módulo `products` (`backend/src/modules/products/`) que agrupa `ProductsController`+`CategoriesController`+`UnitsController` — mismo criterio de agrupación que `RolesModule`+`PermissionsController` (HU-02), porque `Category`/`Unit` solo existen para que `Product` los referencie. **Primera vez que se usa deliberadamente el carve-out de `convenciones.md`** ("CRUD triviales pueden empezar sin las 4 capas"): **sin capa `domain/`** — los tres repositorios (`ProductPrismaRepository`/`CategoryPrismaRepository`/`UnitPrismaRepository`) son clases concretas inyectadas directo, sin interfaz de puerto ni `Symbol`. Endpoints: `GET/POST /products`, `PATCH /products/:id` (incl. `status: discontinued` para "borrado" lógico, ADR-22 — sin `requiresBatch`/`imageUrl`, fuera de alcance de HU-09/HU-26/27); mismo patrón para `/categories` y `/units` (con `status: active/inactive`). **Desviación deliberada del patrón HU-02/03**: los `GET` van **sin** `@RequirePermission()` — son "cualquier autenticado" según la spec literal del plan maestro (sección 7.4), a diferencia de `GET /roles`/`GET /users` que sí exigían permiso. Solo `POST`/`PATCH` llevan `@RequirePermission('products'\|'categories'\|'units', 'create'\|'update')`. Reusa `isForeignKeyViolation()`/`isUniqueConstraintViolation()` ya existentes (HU-03/HU-02) para validar `unitId`/`categoryId` y nombres duplicados de catálogo. Bootstrap: `seed.ts` extendido con `products:create/update`, `categories:create/update`, `units:create/update` (**sin** `:read`, ya que los GET no lo necesitan — habría quedado inerte) otorgados al Administrador. Sin migración nueva (`Category.name`/`Unit.name` ya tenían `@unique` desde TT-23). **Sin ADR nuevo** — todas las decisiones de esta HU ya estaban cubiertas por ADR-17 (hexagonal solo donde hay lógica real), ADR-18 (fronteras entre módulos), ADR-22 (borrado lógico vía `status`) y ADR-25 (patrón de bootstrap de permisos); no hubo ninguna decisión nueva que documentar. Frontend: carpeta `features/products/` (ya reservada en `convenciones.md`), `ProductsService` separado de `CatalogService` (Category+Unit juntos, mismo criterio de agrupación que el backend), componentes `products-list`/`product-form` (crear+editar, con selects de unidad/categoría que incluyen un flujo inline "+ Crear nueva unidad/categoría" — sin modal, sin dependencia nueva — para el caso común de dar de alta un valor de catálogo mientras se crea un producto, tal como pedía la nota de UX de la propia HU) y `categories-list`/`units-list` (lista + alta inline + activar/desactivar por fila — el único punto de entrada real para `PATCH /categories\|units/:id`). Rutas `/products`, `/categories`, `/units` sin `data.roles` (el rol "Admin Inventario" no existe sembrado, y el GET es "cualquier autenticado" de todas formas). Ajuste menor a `product.model.ts`/`category.model.ts` ya existentes de antes de HU-01 (`description`/`category` pasan de opcionales a `\| null`, para que el tipo sea honesto con lo que el backend realmente devuelve). **Verificado**: backend — `npm run lint`/`build`/`test` en verde (40 test suites, 127 tests: TDD de los 9 use-cases + 3 controllers nuevos + BDD nuevo de 3 escenarios en `manage-products-catalog.feature`); frontend — `npm run lint`/`build`/`ng test --browsers=ChromeHeadless` en verde (28 tests). **Verificación manual real, local** (backend levantado con `start:dev`): admin crea unidad "Kilogramo" → `201`; crea categoría "Alimentos" → `201`; crea producto "Arroz" referenciando ambas → `201` con objetos anidados completos; `unitId` inexistente → `400`; categoría duplicada → `409`; `PATCH /products/:id` con `status: discontinued` → `200`; usuario "Comprador" (sin `products:create`, reusado de verificaciones anteriores) intenta crear un producto → **`403`**, pero el mismo usuario **sí puede `GET /products`** → `200`, confirmando en vivo la desviación deliberada "cualquier autenticado" para lectura. Frontend (`ng serve`, servidor viejo detectado y matado antes de repetir la prueba) compiló limpio con los chunks nuevos (`products-list-component`, `product-form-component`, `categories-list-component`, `units-list-component`). **No se pudo verificar visualmente en navegador real** (misma limitación de entorno que las tres HU anteriores, sin `chromium-cli`/Playwright). **Cierre** (2026-08-15): PR #69 mergeado a `staging`; `npm run prisma:seed:staging` corrido contra Neon staging sin error; backend real de Render verificado en vivo (`curl /health` → `200 OK`, `database: ok`). Con esto se cierra la tanda completa (HU-01→02→03→28). |
| HU-06 (crear ubicaciones/salas) | ✅ Mergeada a `staging` (PR #70), cerrada de punta a punta: seed corrido en Neon staging (`locations:read/create/update` otorgado al Administrador) y verificado contra el backend real de Render (`/health` → `200 OK`, `database: ok`). Primera HU de la segunda tanda de MVP1. Backend: nuevo módulo `locations` (`backend/src/modules/locations/`), CRUD trivial sin capa `domain/` (mismo carve-out de `convenciones.md` que HU-28). **A diferencia de HU-28**: el plan maestro (sección 7.4) marca los tres endpoints `/locations` — GET incluido — como "Admin Inventario", así que `GET` también lleva `@RequirePermission('locations','read')` (mismo criterio que HU-02/03, no el de "cualquier autenticado" de HU-28). Modelo `Location` ya existía en el schema (jerarquía `parentId`/`children` self-relation, `LocationStatus`); se agregó `@@unique([parentId, name])` vía migración `20260815000000_add_location_name_unique_per_parent`. **Detalle técnico documentado aquí, sin ADR propio**: Postgres trata cada `parent_id NULL` como distinto, así que ese índice compuesto NO deduplica nombres a nivel raíz por sí solo — `CreateLocationUseCase`/`UpdateLocationUseCase` compensan con un chequeo explícito (`findByParentAndName`) antes de insertar/actualizar; el índice de DB queda como red de seguridad para el caso con padre (y ante una carrera). Guard mínimo agregado: una ubicación no puede ser su propio padre (`parentId === id` → `400`). **Decisión de alcance tomada sin confirmar con el usuario**: el plan maestro describe `GET /locations` como listado "(árbol)"; se implementó como lista plana paginada (TT-19 estándar, igual que el resto de la API) — el frontend arma la jerarquía visualmente a partir de `parentId` — en vez de inventar un endpoint de árbol recursivo sin precedente en el codebase. Es reversible si se prefiere lo contrario. Reusa `isForeignKeyViolation()`/`isUniqueConstraintViolation()` ya existentes. Bootstrap: `seed.ts` extendido con `locations:read/create/update` para el Administrador. Frontend: `features/locations/` (`LocationsService` + `locations-list` con alta inline, selector de ubicación padre, y activar/desactivar por fila — no hay `DELETE`, ADR-22), ruta `/locations` con `data.roles: ['Administrador']` (mismo criterio que `/roles`/`/users`, porque aquí el GET también está gateado, a diferencia de `/products`). **Verificado**: backend — `npm run lint`/`build`/`test` en verde (46 test suites, 152 tests: TDD de 3 use-cases + controller nuevos + BDD nuevo de 5 escenarios en `manage-locations.feature`); frontend — `npm run lint`/`build`/`ng test --browsers=ChromeHeadless` en verde (31 tests). **Verificación manual real, local** (backend levantado con `start:dev`, migración aplicada con `prisma migrate deploy` — `prisma migrate status` confirmó sin drift): crear ubicación raíz "Sede Central" → `201`; crear ubicación hija "Bodega A" bajo esa raíz → `201`; nombre duplicado bajo el mismo padre → `409`; `parentId` inexistente → `400`; desactivar la raíz aunque tenga hijos asociados (nunca hay `DELETE`) → `200` con `status: inactive`; sin token → `401`; `GET /locations` con token → `200` paginado; usuario con rol sin `locations:create`/`locations:read` (creado ad hoc para la prueba) → **`403`** en ambos, confirmando en vivo que el GET también está protegido. Frontend (`ng serve`) sirvió `/locations` → `200`, chunk lazy `locations-list-component` presente. **No se pudo verificar visualmente en navegador real** (misma limitación de entorno que HU-01/02/03/28, sin `chromium-cli`/Playwright). **Cierre** (2026-08-16): PR #70 mergeado a `staging`; `npm run prisma:seed:staging` corrido contra Neon staging sin error; backend real de Render verificado en vivo (`curl /health` → `200 OK`, `database: ok`). |
| HU-07 (asociar inventario a ubicación) | ✅ Mergeada a `staging` (PR #72, CI en verde). El plan maestro (sección 7.4) no le asigna endpoint propio a HU-07 — solo `POST/GET /inventory/movements` (HU-08) y `GET /inventory/stock` (HU-10). **Decisión confirmada con el usuario tras un conflicto real** (documentada en **ADR-27**): un `POST /inventory/stock` propio tendría que escribir `LocationStock` sin pasar por `InventoryMovement`, violando la regla ya establecida en `convenciones.md`. Se optó por adelantar el motor completo de `POST /inventory/movements` bajo HU-07 (transaccional, `withOptimisticLock()` de TT-17 y `@Idempotent()`/`IdempotencyInterceptor` de TT-18 — **primer consumidor real de ambos**), habilitando **solo el tipo `in`** (`@IsIn(['in'])` en el DTO); HU-08 solo tiene que aflojar ese validador para agregar `out`/`transfer_in`/`transfer_out`/`adjustment` sobre la misma base. Nuevo módulo `inventory` (`backend/src/modules/inventory/`) con **arquitectura hexagonal completa** (`domain/application/infrastructure`) — a diferencia de `locations`/`products`, `convenciones.md` la exige explícitamente aquí por no ser CRUD trivial. `domain/movement-request.entity.ts` (`MovementRequest`, entidad con comportamiento ADR-17) calcula el delta de stock según tipo y rechaza los no soportados todavía. Repositorio: el lookup de `LocationStock` existente usa `findFirst` (no `findUnique` con la clave compuesta — el tipo generado por Prisma exige `batchId: string`, sin `null`, mismo issue ya resuelto en HU-06); un conflicto de versión dentro de la transacción lanza un error interno para forzar el rollback completo de Prisma (incluyendo el `InventoryMovement` recién creado), no solo un valor `null` sin deshacer nada — de lo contrario quedaría un movimiento comiteado huérfano mientras el stock no se actualiza. Existencia de `locationId`/`productId` validada vía FK (`P2003`, mismo patrón que HUs previas); que la ubicación esté **activa** se valida explícitamente (ninguna constraint de Postgres lo garantiza). `GET /inventory/stock` construido pero deliberadamente "básico" (ids planos, sin filtros ni datos anidados) — HU-10 lo enriquece. Nuevo decorador reusable `@CurrentUser()` (`common/decorators/`) para extraer `req.user.id` sin que el cliente pueda falsificar quién registra el movimiento. Bootstrap: `seed.ts` extendido con `inventory:create` para el Administrador (sin `:read`, `GET /inventory/stock` es "cualquier autenticado", mismo criterio que HU-28). Sin migración nueva (todas las tablas del ledger ya existían desde el diseño original + TT-17/TT-18). **Verificado**: backend — `npm run lint`/`build`/`test` en verde (52 test suites, 178 tests: TDD de la entidad de dominio + 2 use-cases + repositorio con transacción mockeada (incluye el caso de reintento real por conflicto de versión) + controller, BDD nuevo de 6 escenarios en `manage-inventory-movements.feature`). **Verificación manual real, local** (backend levantado con `start:dev`): sin header `Idempotency-Key` → `400`; movimiento `in` de 10 unidades → `201`; repetir la misma `Idempotency-Key` → misma respuesta `201` sin duplicar stock (`GET /inventory/stock` confirma `10`, no `20`); nuevo movimiento con key distinta → stock sube a `20` (confirmado contra Postgres real, no mock); `locationId` con formato válido pero inexistente → `400`; ubicación desactivada → `400` (`"locationId refers to an inactive location"`); usuario sin permiso `inventory:create` → `403`. Sin frontend en esta HU (no hay HU de negocio que consuma `POST /inventory/movements` desde una pantalla todavía; HU-08/HU-10 lo necesitan). **Cierre** (2026-08-16): PR #72 mergeado a `staging`; `npm run prisma:seed:staging` corrido contra Neon staging sin error; backend real de Render verificado en vivo (`curl /health` → `200 OK`, `database: ok`). |
| HU-08 (registrar entradas/salidas de stock) | ✅ Mergeada a `staging` (PR #74, CI en verde). Tercera HU de la segunda tanda de MVP1. **Dos decisiones de diseño confirmadas con el usuario antes de programar** (documentadas en **ADR-28**): (1) stock insuficiente **bloquea** la operación con `409` — nunca se permite que `LocationStock.quantity` quede negativo; (2) "traslado" es **una sola llamada atómica** (`transfer_out`+`transfer_in` en una transacción) — nunca queda un lado sin el otro. `InventoryPrismaRepository` gana `applyStockChange()` privado compartido (usado por `registerMovement` y el nuevo `registerTransfer`) que agrega el piso en cero: si `delta < 0` y no hay fila `LocationStock` o la existente no alcanza, lanza `InsufficientStockError` (nueva, exportada) **antes** de intentar el update — no hace falta re-chequear tras un conflicto de versión porque un escritor concurrente que cambia la cantidad también incrementa `version`, así que el reintento de `withOptimisticLock()` re-evalúa el piso con datos frescos en cada intento. A diferencia de `VersionConflictError` (interna, siempre se reintenta), `InsufficientStockError` **nunca se reintenta** — las dos use-cases (`RegisterMovementUseCase`/`RegisterTransferUseCase`) la traducen a `409 ConflictException`. `MovementRequest` (dominio) ahora calcula `in`/`out`/`adjustment` (con `direction: increase|decrease` nuevo en el DTO, requerido solo para `adjustment` vía `@ValidateIf`) — `transfer_in`/`transfer_out` NO pasan por esta entidad, sus deltas son inequívocos y se calculan directo en `registerTransfer()`. **`type: 'transfer'` es una categoría de cara al cliente, no un `MovementType` crudo**: el DTO acepta `'in'|'out'|'adjustment'|'transfer'` — nunca `transfer_in`/`transfer_out` directos, esos dos solo existen como las dos filas del ledger que un traslado crea internamente. Cuando `type === 'transfer'`, exige `destinationLocationId` (`locationId` pasa a ser el origen); `RegisterTransferUseCase` valida que origen y destino existan, estén activos, y sean distintos (`400` si no) antes de la transacción. Sigue siendo **un solo endpoint** `POST /inventory/movements` (coincide con la tabla del plan maestro) — el controller solo enruta internamente a `RegisterTransferUseCase` cuando corresponde. Sin migración nueva (el enum `MovementType` ya traía los 5 valores desde el diseño original). Frontend nuevo: `features/inventory/` — un único formulario (`movement-form`) cubre las 4 categorías, mostrando `direction`/`destinationLocationId` solo cuando corresponde; `InventoryService.registerMovement()` genera un `Idempotency-Key` (`crypto.randomUUID()`) por envío — primer consumidor real de un endpoint `@Idempotent()` desde el frontend. Ruta `/inventory` gateada con `data.roles: ['Administrador']` (única pantalla ahí es de escritura; HU-10 revisará el gate al agregar la de consulta). **Verificado**: backend — `npm run lint`/`build`/`test` en verde (53 test suites, 203 tests: TDD extendido de la entidad de dominio (out/adjustment) + repositorio (insuficiencia de stock + traslado atómico, incluyendo verificación de rollback) + 2 use-cases (uno nuevo, `RegisterTransferUseCase`) + controller, BDD extendido a 12 escenarios en `manage-inventory-movements.feature`); frontend — `npm run lint`/`build`/`ng test --browsers=ChromeHeadless` en verde (33 tests). **Verificación manual real, local** (backend levantado con `start:dev`): `in` 20 → `201`; `out` 5 → `201`, stock en `15`; `out` 100 (excede) → `409`, stock sigue en `15` (no tocado); `adjustment increase` 3 → `18`; `adjustment decrease` 3 → vuelve a `15`; `adjustment` sin `direction` → `400`; traslado de 6 unidades entre dos ubicaciones → `201`, origen baja a `9`, destino sube a `6`; traslado que excede el origen (pedir 100 con 9 disponibles) → `409`, **ambas ubicaciones confirmadas sin cambios** (prueba real del rollback atómico contra Postgres, no mock); traslado con mismo origen y destino → `400`. Frontend (`ng serve`) sirvió `/inventory` → `200`, chunk lazy `movement-form-component` presente. **No se pudo verificar visualmente en navegador real** (misma limitación de entorno que HUs anteriores, sin `chromium-cli`/Playwright). **Cierre** (2026-08-16): PR #74 mergeado a `staging`, CI en verde; no requirió re-seed en Neon staging (reusa `inventory:create` de HU-07, sin permiso nuevo); backend real de Render verificado en vivo (`curl /health` → `200 OK`, `database: ok`). |
| HU-09 (producto requiere lote/vencimiento) | ✅ Mergeada a `staging` (PR #76, CI en verde). Cuarta HU de la segunda tanda de MVP1. Su criterio de aceptación en Trello era insuficiente ("depende del producto", sin diseño) — consultado con el usuario, se optó por **gestión completa de lotes**, no solo el flag suelto. Nuevo `BatchesController` (`POST/GET /inventory/batches/:productId`, plan sección 7.4, ambos "Admin Inventario" — a diferencia de `GET /inventory/stock`, así que se sembró `inventory:read` por primera vez) + `BatchPrismaRepository`/`CreateBatchUseCase`/`ListBatchesUseCase` dentro del módulo `inventory` existente — trivial CRUD, sin `domain/` propio (la regla de negocio real vive en los use-cases: "solo si el producto lo requiere"). **Validación real agregada a `RegisterMovementUseCase`/`RegisterTransferUseCase`** vía helper compartido `validateProductAndBatch()`: si `Product.requiresBatch === true`, el movimiento exige `batchId`; si se provee un `batchId`, debe existir y pertenecer al `productId` del movimiento (nada a nivel de DB lo garantiza — `Batch.productId` e `InventoryMovement.batchId` son FKs independientes). Esto reemplaza el catch de `isForeignKeyViolation()` para `productId` en ambas use-cases (ya redundante — `findProductRequiresBatch()` valida su existencia explícitamente antes de intentar el insert). **Hallazgo real durante la implementación**: `CreateProductDto`/`UpdateProductDto` (HU-28) excluían `requiresBatch` a propósito — el propio comentario en el código decía *"requiresBatch (HU-09)... intentionally not here"* — sin ese campo, el flag nunca fue editable desde la API pese a existir en el schema desde el diseño original. Se completó ahora: DTOs, `ProductPrismaRepository`, ambos use-cases de `products`, y el checkbox correspondiente en `product-form` del frontend. Sin migración nueva (`Batch`, `Product.requiresBatch` y sus FKs ya existían desde el diseño original). Frontend: `features/inventory/batches-list/` (pantalla nueva: elegir producto que requiere lote, listar sus lotes, alta inline) + `movement-form` extendido con selector de lote condicional (visible solo si el producto elegido tiene `requiresBatch`). **Dos gotchas de entorno durante la verificación manual** (no son cambios de código, quedan documentados por si se repiten en una próxima sesión): (1) procesos `node` huérfanos de verificaciones anteriores seguían escuchando en el puerto 3000 pese a `TaskStop` — un restart del servidor parecía "limpio" (compilaba sin errores) pero en realidad seguía sirviendo el proceso viejo, devolviendo `404` en rutas nuevas hasta matar el PID a mano vía PowerShell; (2) se sospechó brevemente que el **orden de controllers** en `@Module({ controllers: [...] })` causaba el mismo síntoma (razonamiento equivocado por analogía con `app.use()` de Express) — se revirtió tras confirmar que NestJS registra rutas exactas por método, no por prefijo, así que el orden no importa. **Verificado**: backend — `npm run lint`/`build`/`test` en verde (58 test suites, 227 tests: TDD nuevo de `BatchPrismaRepository`/`CreateBatchUseCase`/`ListBatchesUseCase` + tests extendidos de `RegisterMovementUseCase`/`RegisterTransferUseCase` (validación de lote) + tests extendidos de `create-product`/`update-product` use-cases (`requiresBatch`), BDD nuevo de 7 escenarios en `manage-product-batches.feature`); frontend — `npm run lint`/`build`/`ng test --browsers=ChromeHeadless` en verde (35 tests). **Verificación manual real, local** (backend levantado con `start:dev`, tras resolver el proceso huérfano): crear lote para producto sin `requiresBatch` → `400`; `PATCH /products/:id` con `requiresBatch:true` → `200` (confirma que el hallazgo de DTO quedó resuelto); crear lote para producto que sí lo requiere → `201`; listar lotes del producto → `200`, incluye el lote creado; movimiento `in` sin `batchId` para producto con `requiresBatch` → `400`; mismo movimiento con `batchId` correcto → `201`; movimiento usando un `batchId` de **otro** producto → `400` (`"batchId does not belong to productId"`); usuario sin `inventory:create` → `403` al crear lote; usuario sin `inventory:read` → `403` al listar lotes. 9/9 casos correctos. **No se pudo verificar visualmente en navegador real** (misma limitación de entorno que HUs anteriores, sin `chromium-cli`/Playwright). **Cierre** (2026-08-16): PR #76 mergeado a `staging`, CI en verde; `npm run prisma:seed:staging` corrido contra Neon staging sin error (agrega `inventory:read`); backend real de Render verificado en vivo (`curl /health` → `200 OK`, `database: ok`). |
| HU-10 (consultar stock actual) | ✅ Mergeada a `staging` (PR #78, CI en verde) y verificada también en producción tras la sincronización `main` del 2026-08-17 (ver ADR-30). Última HU de MVP1 — **con esto MVP1 queda completo**. Enriquece `GET /inventory/stock`, dejado deliberadamente "básico" por HU-07 (ADR-27): (1) **filtros** `productId`/`locationId` opcionales, nuevo `StockQueryDto extends PaginationQueryDto` (plan sección 7.4: "filtrable por producto/ubicación"); (2) **respuesta anidada** — `StockResponseDto.product`/`.location` pasan de ids planos a `{id, name}`, vía `include` de Prisma en `findStockPaginated()` (antes solo `LocationStock` crudo). `ListStockUseCase` pasa los filtros del DTO directo al repositorio; `where` se arma condicionalmente (`{}` si no hay filtros) y se reusa igual en `findMany`/`count` dentro de la misma `$transaction`. **Reestructuración del gate de rutas del frontend**: hasta HU-09, `/inventory` tenía un solo gate a nivel del padre (`data.roles: ['Administrador']`) que aplicaba a todo el subárbol — dejaba de tener sentido en cuanto `GET /inventory/stock` es "cualquier autenticado" (a diferencia de `POST /inventory/movements` y `/inventory/batches`, que sí son "Admin Inventario"). El gate bajó de `app.routes.ts` a cada ruta hija dentro de `inventory.routes.ts`: `''` (formulario) y `'batches'` mantienen `data.roles: ['Administrador']`, `'stock'` (nueva) solo lleva `canActivate: [roleGuard]` sin restricción de rol — mismo patrón ya usado por `/products`. Frontend: pantalla nueva `features/inventory/stock-list/` con dos `<select>` de filtro (producto, ubicación) que recargan la tabla al cambiar; tabla muestra nombre de producto/ubicación (no ids) gracias al enriquecimiento del backend. Sin migración nueva. **Verificado**: backend — `npm run lint`/`build`/`test` en verde (58 test suites, 231 tests: TDD nuevo de `findStockPaginated` con/sin filtros en el repositorio + `ListStockUseCase` extendido, BDD nuevo de 1 escenario de filtrado en `manage-inventory-movements.feature`); frontend — `npm run lint`/`build`/`ng test --browsers=ChromeHeadless` en verde (36 tests). **Verificación manual real, local**: `GET /inventory/stock` sin filtros → `200`, todas las filas con `product`/`location` anidados; filtrado por `locationId` → solo la fila de esa ubicación; filtrado por `productId` → todas las filas de ese producto entre ubicaciones; usuario sin ningún permiso → `200` en `GET /inventory/stock` (confirma "cualquier autenticado") pero **`403`** en `POST /inventory/movements` (confirma que el resto del gate sigue intacto). 5/5 casos de backend correctos. Frontend (`ng serve`) sirvió `/inventory/stock` y `/inventory/batches` → `200` ambas. **No se pudo verificar visualmente en navegador real** (misma limitación de entorno que HUs anteriores, sin `chromium-cli`/Playwright). **Cierre** (2026-08-17): PR #78 mergeado a `staging`, CI en verde; sin permiso nuevo que sembrar. Verificado además en producción real tras la sincronización `staging` → `main` (ver ADR-30 e "Incidente de producción" abajo): `/inventory/stock` responde en el backend real de Render con el código ya vigente. **Con el cierre de esta HU, MVP1 queda completo** — siguiente hito: definir aislamiento staging/producción (ver arriba), luego UAT con el usuario funcional. |

| HU-04 (registrar proveedor) | ✅ Implementada (2026-08-19), rama `feature/hu-04-register-supplier`. **Primera HU de MVP2**, orden acordado con el usuario: HU-04 → HU-13 → HU-05 → HU-14 (WIP limit 1, una rama/un PR por HU). Backend: nuevo módulo `suppliers` (`backend/src/modules/suppliers/`), CRUD sin capa `domain/` — mismo carve-out de `convenciones.md` que `locations`/`products` (`Supplier` no tiene lógica de dominio compleja, la única regla real es la unicidad condicional de abajo). El plan maestro (sección 7.4) marca las 3 rutas como "Comprador" mínimo, GET incluido (a diferencia del GET abierto de `/products`) — igual que "Admin Inventario" en HU-06/07/09, ese rol no existe realmente sembrado; se sembró `suppliers:read/create/update` sobre "Administrador" (`PermissionsGuard` valida el permiso, no el nombre del rol — un admin puede crear el rol "Comprador" de verdad desde `/roles`, ya construido en TT-24 fase 9). **Regla de negocio real**: "no se permite duplicar NIT entre proveedores activos" — un `@@unique` simple de Prisma no puede expresar "solo si status=active", así que se resolvió en dos capas, mismo patrón que HU-06 usó para `parentId+name`: (1) chequeo explícito en `CreateSupplierUseCase`/`UpdateSupplierUseCase` vía `findActiveByTaxId()`; (2) índice parcial de Postgres escrito a mano (`CREATE UNIQUE INDEX ... WHERE status = 'active'`, migración `20260819040000_add_supplier_tax_id_active_unique`) como red de seguridad ante una carrera — sin equivalente en la sintaxis de `schema.prisma` (no soporta índices parciales), documentado con un comentario en el modelo `Supplier` para que no se lea como drift. La regla de "reactivar" también se cubre: `UpdateSupplierUseCase` revisa el conflicto tanto cuando cambia `taxId` como cuando `status` vuelve a `active` (no solo cuando cambia el NIT), porque el resultado de la operación es lo que importa, no qué campo se tocó — cubierto con test específico. Nunca hay `DELETE` (ADR-22, mismo patrón `status` que `Location`/`Product`) — "un proveedor con compras no se puede eliminar, solo desactivar" queda satisfecho estructuralmente por la ausencia del endpoint, no por lógica adicional. Frontend: `features/suppliers/` — reemplaza el placeholder que existía desde antes de HU-01 (`[PENDING: connect to SuppliersService and GET /suppliers]`) por `suppliers-list`+`supplier-form` (patrón lista+formulario aparte, igual que `products`/`users`, no alta inline — la ficha de proveedor tiene más campos que categorías/unidades). Nueva sección de navegación "Compras" en `nav-items.ts` (crecerá con HU-13/05/14), gateada a `data.roles: ['Administrador']` en `app.routes.ts` por el mismo motivo que el backend: "Comprador" no existe sembrado todavía. **Verificado**: backend — `npm run lint`/`build`/`test` en verde (65 test suites, 263 tests: TDD de `CreateSupplierUseCase`/`UpdateSupplierUseCase`/`ListSuppliersUseCase`/controller + BDD nuevo de 6 escenarios en `manage-suppliers.feature`, incluye NIT duplicado entre activos rechazado, NIT repetido con un inactivo permitido, y el 403 sin permiso); frontend — `npm run lint`/`build`/`ng test --browsers=ChromeHeadless` en verde (106 tests, 10 nuevos). **Verificación manual real, local** (backend levantado con `start:dev` contra Postgres local, migración aplicada con `prisma migrate deploy` — `prisma migrate status` confirmó sin drift): crear proveedor solo con nombre → `201`; crear con NIT → `201`; NIT repetido entre activos → `409`; desactivar el proveedor → `200`, `status: inactive`; crear un segundo proveedor con el mismo NIT ya liberado (el primero inactivo) → `201`; intentar reactivar el primero (ahora chocaría con el segundo, activo) → **`409`**, confirmando que el chequeo cubre la reactivación y no solo el cambio de NIT; sin token → `401`; listado paginado → `200`. 8/8 casos correctos. Frontend (`ng serve`) sirvió `/suppliers` y `/suppliers/new` → `200` ambas, chunks lazy `suppliers-list-component`/`supplier-form-component` presentes. **No se pudo verificar visualmente en navegador real** (misma limitación de entorno que toda HU anterior, sin `chromium-cli`/Playwright). **Ampliación de alcance, a pedido explícito del usuario tras la primera entrega**: un proveedor puede ser persona natural (Cédula) o jurídica (NIT) — se agregaron dos catálogos administrables nuevos, mismo patrón TT-23 que `Category`/`Unit`: `DocumentType` (Cédula de ciudadanía/Cédula de extranjería/NIT, sembrados de fábrica por `seed.ts` pero editables desde `/document-types`) y `PersonType` (Natural/Jurídica, `/person-types`). **La regla de unicidad se recalculó**: ya no es "NIT único entre activos" a secas, sino "NIT único entre activos **del mismo tipo de documento**" — una Cédula y un NIT con los mismos dígitos son identificadores distintos, no un choque. La migración `20260819040000` (renombrada a `add_supplier_document_person_type_catalogs`, todavía sin desplegar a ningún entorno compartido, así que se pudo editar en vez de apilar una segunda) ahora crea `document_type`/`person_type`, agrega `documentTypeId`/`personTypeId` a `Supplier`, y el índice parcial pasa a ser `(document_type_id, tax_id) WHERE status='active'`. **Gotcha documentado, mismo que HU-06 ya aceptó para `parentId`**: Postgres trata cada `document_type_id` nulo como distinto de cualquier otro nulo, así que el índice de DB no cubre una carrera entre dos altas concurrentes que **ambas** omiten el tipo de documento — ese caso depende solo del chequeo de aplicación; aceptado como trade-off, no vale la pena una expression-index para un sistema de un admin a la vez. `document-types`/`person-types` quedan gateados igual que `suppliers` (permiso "Comprador" en las 3 rutas, GET incluido) — a diferencia de `Category`/`Unit`, que heredan el GET abierto de `Product`. Frontend: `document-types-list`/`person-types-list` (mismo patrón lista+alta-inline+activar/desactivar que `categories-list`/`units-list`, con conteo de proveedores por tipo calculado en el cliente igual que esas dos lo hacen con productos), `supplier-form` ahora tiene selects de tipo de documento/persona con el mismo atajo inline "+ Crear" que `product-form` usa para unidad/categoría. **Verificado de nuevo tras la ampliación**: backend — 73 test suites, 291 tests (28 nuevos: TDD completo de `DocumentType`/`PersonType` + BDD extendido a 10 escenarios, incluye "el mismo número bajo dos tipos de documento distintos no es un choque"); frontend — 113 tests (7 nuevos). **Verificación manual real, local** (Postgres reseteado y migrado limpio, `prisma migrate status` sin drift, seed corrido — catálogos de fábrica confirmados: 3 tipos de documento, 2 tipos de persona): Cédula "123456" persona natural → `201`; mismo número "123456" pero como NIT (empresa distinta) → `201`, **permitido**; repetir Cédula "123456" de nuevo → `409`; `documentTypeId` inexistente → `400`; crear tipo de documento "Pasaporte" → `201`; `/document-types`/`/person-types` sin token → `401` ambos. 7/7 casos correctos, sumados a los 8/8 de la entrega original. **Incidente real encontrado durante la revisión del usuario (2026-08-19)**: PR #106 (alcance original) se mergeó a `staging` a las 04:33:11Z — **antes** de que este commit con los catálogos de `DocumentType`/`PersonType` llegara a pushearse. El usuario revisó el preview de `staging` y correctamente notó que el formulario seguía mostrando un campo de texto plano en vez de los selects — porque, de hecho, la ampliación nunca había llegado a `staging`, seguía viviendo solo en la rama. Diagnosticado comparando `git log origin/staging..origin/feature/hu-04-register-supplier` (mostró exactamente este commit, ninguno más) contra los deployments reales de Vercel (API `list_deployments`, confirmó el commit exacto detrás de cada alias). **Corregido**: como el PR #106 ya estaba cerrado/mergeado, no se podía reabrir — se abrió **PR #107** desde la misma rama (`feature/hu-04-register-supplier` → `staging`, ahora divergentes de nuevo) con este commit. **Cierre**: PR #107 abierto hacia `staging`, todavía sin mergear — falta que el usuario lo revise/mergee y corra `npm run prisma:migrate:staging` contra Neon (la migración del índice parcial, análoga a lo que pasó con `add_revoked_token`: sin correrla ahí, el backend real seguiría sin el índice aunque el código ya lo espere). |
| HU-13 (registrar compra a proveedor) | ✅ Implementada (2026-08-19), rama `feature/hu-13-register-purchase`. Segunda HU de MVP2. **Vacío real encontrado en el criterio antes de codear, resuelto con el usuario en vez de asumido**: el schema ya traía `Purchase`/`PurchaseItem` diseñados desde el origen (`InventoryMovement.purchaseId` como FK de vuelta, así que el motor de movimientos ya esperaba esto), pero ninguno de los dos tenía un campo de ubicación, y todo movimiento exige `locationId`. Preguntado: ¿una ubicación por compra o una por ítem? El usuario eligió **una por ítem** — un mismo pedido puede reabastecer más de una ubicación. Se agregaron `locationId` (requerido) y `batchId` (opcional) a `PurchaseItem` (migración `20260819060000_add_purchase_item_location_batch`), con relaciones inversas nuevas en `Location`/`Batch`. **El "motor" que no se podía reimplementar vivía en `infrastructure/` de otro módulo**: `InventoryPrismaRepository.applyStockChange()`+la creación del `InventoryMovement` (HU-07/08) es exactamente lo que HU-13 necesita reusar, pero ADR-18 prohíbe que `purchases` importe `infrastructure/` de `inventory`. Solución: esa lógica se extrajo verbatim a `common/utils/inventory-ledger.util.ts` (`createMovementAndApplyStock()`, `InsufficientStockError`, `VersionConflictError`) — mismo lugar que ya usan `optimistic-lock.util.ts`/`prisma-error.util.ts` para utilidades genuinamente transversales — y `InventoryPrismaRepository` se refactorizó para llamarlo en vez de mantener su propia copia (extracción pura, sin cambio de comportamiento: los 70 tests de `inventory` existentes se corrieron después del refactor y siguieron en verde sin tocarlos). **Arquitectura hexagonal completa** (`domain/application/infrastructure`), a pedido explícito del usuario: `PurchaseItemRequest` (Value Object, auto-valida cantidad positiva y precio no negativo) + `PurchaseRequest` (entidad, `getTotalAmount()` suma subtotales) — no decoración, es donde realmente corre el cálculo del total antes de persistir. **La transacción es una sola**: `PurchasePrismaRepository.registerPurchase()` crea el `Purchase`, y por cada ítem — dentro de la misma transacción — busca o crea el lote (si trae `batchNumber`), crea el `PurchaseItem`, y llama `createMovementAndApplyStock()`; un fallo en el ítem 3 de 5 revierte todo, incluidos los ítems 1 y 2 ya escritos en ese intento, y `withOptimisticLock()` reintenta la transacción completa desde cero (mismo patrón que el traslado atómico de HU-08). **"Si el lote no existe todavía, créalo"**: a diferencia de HU-08/09 (`batchId`, debe existir), el ítem de compra manda un `batchNumber` (código de lote humano) — el repositorio busca `(productId, batchNumber)` y crea el lote si no aparece, dentro de la misma transacción; `batchNumber` es obligatorio solo si el producto tiene `requiresBatch` (validado en el use-case, no declarativamente — igual que `validateProductAndBatch` de HU-09, la comprobación necesita el estado real del producto). **Gotcha aceptado, documentado igual que el `documentTypeId` nulo de HU-04**: Postgres nunca hace matchear dos `document_type_id`/aquí no aplica, pero el equivalente sí — no hay índice único en `(productId, batchNumber)`, así que dos compras concurrentes creando el mismo lote nuevo tienen una ventana de carrera mínima; aceptado para un sistema de un admin a la vez, no se agregó constraint nueva para no tocar el diseño de HU-09. `status` se fija en `'received'` al crear (no `'registered'`): esta HU genera los movimientos como parte de registrar, no hay flujo de recepción en dos pasos todavía — el valor `'registered'` del enum queda libre para una HU futura que sí lo necesite. Nuevos endpoints `GET/POST /purchases`, `GET /purchases/:id` (primera vez que una entidad tiene detalle propio en la API, no solo filtrado client-side del listado — el plan lo pide explícito para HU-13), `POST` es `@Idempotent()` (TT-18, a pedido explícito del usuario). Permisos `purchases:read/create` sembrados sobre "Administrador" (sin `:update`, no hay PATCH — una compra no se edita). **Hallazgo real durante la verificación manual, no detectado por los tests mockeados**: crear un lote nuevo con `batchExpiresAt` tiraba `500` — faltaba el `new Date(...)` que `BatchPrismaRepository.create()` (HU-09) sí hace antes de pasarle la fecha a Prisma; corregido para seguir el mismo patrón exacto. Frontend: `features/purchases/` — `purchases-list` (mismo patrón catálogo que `suppliers-list`: fecha, proveedor, cantidad de ítems, total, estado) + `purchase-form`, el **primer formulario multi-fila de la app** (`FormArray`, ninguna pantalla anterior — ni `movement-form` — escribía más de una línea a la vez): selector de proveedor, filas dinámicas de ítem (producto, ubicación, cantidad, precio, número de lote condicional si el producto lo requiere) con agregar/quitar fila, total calculado en vivo. Nueva entrada "Compras" en `nav-items.ts` bajo la sección ya creada por HU-04, mismo gate `data.roles: ['Administrador']` que `/suppliers`. **Verificado**: backend — `npm run lint`/`build`/`test` en verde (80 test suites, 321 tests: TDD de `PurchaseItemRequest`/`PurchaseRequest`/3 use-cases/controller + BDD nuevo de 7 escenarios en `manage-purchases.feature`, incluye "el mismo `batchNumber` reusado no crea un lote duplicado" y "una compra con varios ítems actualiza el stock de cada uno por separado"); frontend — `npm run lint`/`build`/`ng test --browsers=ChromeHeadless` en verde (123 tests, 10 nuevos). **Verificación manual real, local** (Postgres real, backend levantado con `start:dev`, seed corrido, datos base creados vía la API real — proveedor, unidad, dos productos uno con `requiresBatch`, ubicación): compra de un ítem sin lote → `201`, stock sube de 0 a 10; sin header `Idempotency-Key` → `400`; producto con `requiresBatch` sin `batchNumber` → `400`; mismo producto con `batchNumber` nuevo → `201`, lote creado de verdad (confirmado vía `GET /inventory/batches/:productId`); **reusar el mismo `batchNumber`** → `201`, mismo `batchId`, no duplica el lote; proveedor inexistente → `400` (validación de formato UUID); proveedor desactivado → `400` explícito ("refers to an inactive supplier"); `GET /purchases/:id` → `200` con detalle completo, id inexistente (pero UUID válido) → `404`; **compra multi-ítem** (Arroz + Paracetamol con lote) → `201`, total correcto (8+2=10); stock final acumulado tras 4 compras sobre los mismos productos/ubicación → `14` y `10` respectivamente, coincide exactamente con la suma esperada, confirmando que la acumulación entre compras y el reuso del mismo lote funcionan correctamente de punta a punta. 15/15 casos correctos. **No se pudo verificar visualmente en navegador real** (misma limitación de entorno que toda HU anterior, sin `chromium-cli`/Playwright). **Cierre**: PR #109 mergeado a `staging`. Migración/seed contra Neon confirmados por el usuario y reverificados de forma independiente: `dotenv -e .env.staging -- prisma migrate status` → "Database schema is up to date!" (10/10 migraciones, incluida `20260819060000_add_purchase_item_location_batch`, sin drift). Nota: el servicio de Render (`tg-inventory-backend`) solo auto-despliega la rama `main`, no `staging` — sin isolación real de entornos todavía (TT-28 pendiente), la "base de staging" es la misma Neon DB, pero el código desplegado en Render sigue siendo el de `main` hasta el próximo sync; por eso `GET /suppliers`/`/purchases` devuelven `404` contra `tg-inventory-backend.onrender.com` ahora mismo — esperado, no es un incidente. |
| HU-05 (histórico de compras del proveedor) | ✅ Implementada (2026-08-19), rama `feature/hu-05-supplier-purchase-history`. Tercera HU de MVP2. El plan maestro (sección 7.4) le da su propio endpoint, `GET /suppliers/:id/purchases`, distinto del `GET /purchases` genérico de HU-13 — así se construyó: `SupplierPurchaseHistoryController` (`@Controller('suppliers')`, ruta `:id/purchases`) vive en el módulo `purchases` (no `suppliers`) porque la capacidad que envuelve —`GetSupplierPurchaseHistoryUseCase`, `PurchasePrismaRepository`— pertenece ahí; NestJS permite que un controller de un módulo posea una ruta bajo el prefijo de otra feature sin que eso cruce los límites de ADR-18 en ningún sentido. El use-case valida primero que el proveedor exista (`404` si no) vía `findSupplierStatus()`, y delega en `findAllPaginated(skip, take, {supplierId})`, mismo método que ya usaba el listado genérico de HU-13 pero con el filtro que ya estaba anticipado en `PurchaseQueryDto` desde esa HU. Sin `PATCH`/`POST` — es una vista de solo lectura. **Bug real encontrado y corregido durante esta HU, no relacionado a la lógica de negocio**: el primer intento de BDD (`manage-supplier-purchase-history.steps.ts`) fallaba con `0` resultados en vez de los `2` esperados a pesar de sembrar los datos correctamente — la causa: `Test.createTestingModule()` de Nest no aplica el `ValidationPipe({transform:true})` global que sí corre en `main.ts`, así que `@Query() query: PaginationQueryDto` llegaba como objeto plano sin los defaults de clase (`page=1`/`pageSize=20`) aplicados, y `toPrismaSkipTake()` calculaba `skip`/`take` como `NaN`, que `Array.prototype.slice(NaN, NaN)` resuelve como `slice(0, 0)` → vacío. Corregido agregando el mismo `ValidationPipe` (idéntica config a `main.ts`: `whitelist`, `forbidNonWhitelisted`, `transform`) al bootstrap de ese archivo BDD — gap de arnés de pruebas, no un bug real de producción (confirmado aparte: contra Postgres real con `start:dev`, que sí monta el pipe global, el endpoint ya ordenaba y paginaba bien incluso sin este fix). Frontend: nueva pantalla `supplier-purchase-history` (`features/suppliers/`), mismo patrón catálogo+paginación que `purchases-list`, alcanzable desde un enlace "Ver compras" nuevo en cada fila de `suppliers-list`; como no existe `GET /suppliers/:id` en la API, el nombre del proveedor se resuelve igual que `supplier-form.component.ts` ya lo hacía (listado completo + búsqueda por id en cliente). Ruta `suppliers/:id/purchases`, mismo gate de rol que el resto de `SUPPLIERS_ROUTES`. **Verificado**: backend — `npm run lint`/`build`/`test` en verde (83 test suites, 330 tests: 4 nuevos de `GetSupplierPurchaseHistoryUseCase` + 1 de controller + 4 escenarios BDD nuevos en `manage-supplier-purchase-history.feature`, incluye orden descendente por fecha, estado vacío, `404` de proveedor inexistente y `403` sin permiso); frontend — `npm run lint`/`build`/`ng test --browsers=ChromeHeadless` en verde (128 tests, 5 nuevos). **Verificación manual real, local** (Postgres real, backend levantado con `start:dev`, proveedor y dos compras nuevas creados vía la API real con fechas distintas): histórico sin parámetros de paginación explícitos → `200`, 2 ítems, orden correcto (la compra más reciente primero, total 60 antes que 50) — confirma que el gap de `ValidationPipe` era exclusivo del arnés BDD, no del backend real; mismo resultado con `?page=1&pageSize=20` explícito; proveedor sin compras → `200`, `items: []`, `total: 0`; proveedor inexistente → `404`; sin token → `401`. 5/5 casos correctos. **No se pudo verificar visualmente en navegador real** (misma limitación de entorno que toda HU anterior). **Cierre**: PR pendiente de abrir hacia `staging` — falta que el usuario lo revise/mergee y corra `npm run prisma:migrate:staging`/`prisma:seed:staging` contra Neon (esta HU no agrega migración ni permisos nuevos — reusa `purchases:read` ya sembrado en HU-13 — así que ese paso es solo para mantener el hábito, no hay drift esperado). Sigue **HU-14 — Comparativa de precios**, DoR ya resuelto por el usuario: dos vistas, ambas tabla (no gráfico) — comparación de precio de un producto entre proveedores, y comparación de precio promedio mensual entre hasta 3 proveedores seleccionados. |


### Incidente de producción (2026-08-17): `main` congelado en un commit pre-auth, login roto

El mismo día del cierre de HU-10, el usuario reportó que el login no funcionaba en producción (`https://tg-inventory-app.vercel.app`) al empezar pruebas de UAT. Diagnóstico completo, causa raíz y solución documentados en **ADR-30** (`docs/esp/decisiones.md`) — resumen: el commit realmente desplegado en Render era de antes de HU-01 (solo `HealthModule` en `app.module.ts`), porque Render solo despliega `main` y casi todo el trabajo de esta iteración vivía solo en `staging`; el único intento de sincronizar `main` (PR #64) había fallado el build sin que nadie lo notara (`/health` seguía en verde). Corregido con PR #79 (`prisma generate` en `postinstall`) + la rama `sync/staging-to-main-mvp1` (merge real `staging`→`main`, sin conflictos). Verificado en producción: login funciona, dashboard de admin accesible. **Sigue pendiente** la pregunta de fondo que motivó el reporte: staging y producción comparten el mismo backend Render y la misma Neon DB — no hay aislamiento real para poder certificar UAT sin tocar producción.

### Qué se encontró y arregló al verificar CI (TT-07)

CI nunca había corrido de verdad antes de esto — el PR de prueba (#13) reveló varios bugs reales en el esqueleto, ya arreglados:
- **Backend**: `npm audit --audit-level=critical` fallaba por una vulnerabilidad crítica real en `node-tar`, alcanzable vía `bcrypt` (dependencia de producción) → `@mapbox/node-pre-gyp` → `tar`. Se subió `bcrypt` a `6.0.0` (sin código que lo use todavía, cero riesgo real). El gate de auditoría (HU-24) ahora usa `--omit=dev` en ambos workflows para no bloquear por vulnerabilidades de herramientas de build (webpack/Angular CLI) que no llegan a producción.
- **Frontend**: `package-lock.json` estaba desincronizado de `package.json` (`npm ci` lo rechazaba) — regenerado. No existía ni `.eslintrc.js` ni el target `lint` en `angular.json` — `ng lint` no tenía nada que correr. No existía ni un solo `.spec.ts` — el step de Tests fallaba por config vacía. Se agregó `.eslintrc.js`, el target de lint, y un spec mínimo de `AppComponent`.
- **Ambos**: `tsconfig.json` no tenía `rootDir` explícito (deprecado en TS6) y el backend tenía un `baseUrl` sin usar (deprecado en TS7) — corregido. Los dos workflows tenían su job llamado igual (`test`), así que GitHub reportaba dos checks indistinguibles por nombre — se les puso nombre explícito (`backend` / `frontend`) para que branch protection pueda exigirlos por separado el día que se configure.

Verificado con `npm run lint`, `npm run build` y `npm run test` localmente en ambos proyectos, más los checks reales de GitHub Actions en los PR #13 y #14.

### Backlog de HU (30, en la columna `Backlog`)

Formato base consistente en las 30 (rol/acción/beneficio). ~10 todavía no tienen un criterio de aceptación verificable, solo notas de contexto — **no es bloqueante**: se completa cuando cada una entra a Definition of Ready, justo antes de moverla a "En progreso" (ver `docs/esp/flujo-de-trabajo.md`). No hace falta resolverlo ahora.

**Lo que ya existe en código, ya subido a GitHub**:
- Backend NestJS: estructura hexagonal, `schema.prisma` completo (19 entidades, en inglés), guards/decoradores de RBAC, health-check
- Frontend Angular: estructura por features (todas en inglés), feature `suppliers` como plantilla de referencia, ahora con lint y un spec base funcionando
- `docker-compose.yml`, workflows de CI (verificados y en verde), `dependabot.yml` (agrupado por ecosistema)
- `frontend/public/images/Logo.png` — logo real (reemplazó al placeholder, TT-24 fase 1)

## Qué leer, y en qué orden, si retomas esto en una conversación nueva

1. **Este archivo**
2. `docs/esp/PRD.md` — qué se está construyendo y por qué
3. `docs/esp/plan-inicial-proyecto-inventario.md` — el documento maestro
4. `docs/esp/TRD.md` — resumen técnico
5. `docs/esp/convenciones.md` y `docs/esp/decisiones.md` — antes de tocar código

## Tablero de Trello

https://trello.com/b/BS5tzENy/sistema-de-control-de-inventario — TT-01 a TT-10 (bloque original), TT-14 a TT-22 (revisión de arquitectura) y TT-23 (catálogos administrables) están todas movidas a `Hecho`, cada una con evidencia real en su descripción.

---

# Project status — read this first

> Last updated (2026-08-17): **MVP1 fully complete and closed end to end** (HU-06→07→08→09→10, PRs #70/#72/#74/#76/#78, all merged into `staging`). **Production incident found and fixed the same day**: `main` had been frozen since before HU-01 (the commit actually deployed on Render only had `HealthModule`, no `AuthModule`, nothing business-related) — login returned `404` in production. Cause: Render only auto-deploys `main`, and the one attempt to sync it with `staging` (PR #64) had silently failed its build (missing `prisma generate`). Fixed with **PR #79** (`postinstall: prisma generate` in `backend/package.json`) + a **real `staging` → `main` sync** (branch `sync/staging-to-main-mvp1`, no conflicts, verified locally before pushing: backend 58 suites/231 tests/lint green, frontend build green). Verified in production after the merge: `POST /auth/login` → `401` with a wrong password (no longer `404`), the user confirmed a real login into the dashboard. Full detail in **ADR-30**. **Still unresolved**: the user's underlying UAT concern — staging and production still share the same Render backend and the same Neon DB (hardcoded `apiUrl`, identical in `environment.ts`/`environment.staging.ts`); a real isolation strategy still needs to be decided before any UAT certification. **Also, three improvements came out of this UAT round and are now closed**: (1) a real navigation shell (`mat-sidenav`/`mat-toolbar` — none existed before, every screen needed its URL typed by hand) plus a login screen redesigned with Angular Material; (2) CORS relaxed to accept Vercel preview origins alongside production (**ADR-31**) — previously only one fixed origin passed, breaking login on any preview; (3) a real `POST /auth/logout` with token invalidation via a `jti` blocklist (**ADR-32**), at the user's explicit request — reverses ADR-24's "client-only" decision. All verified locally (lint/build/tests green in both projects, plus real manual verification against Postgres for logout: login → logout → reuse the same token → `401`). **Still needed**: merge these PRs and run `npm run prisma:migrate:staging` on Neon for the `add_revoked_token` migration (no new permission needed, but a schema migration is).

> **TT-24 (new Trello card, inside MVP1) — implement the Claude Design mockup, phase 0 in progress.** The user exported the mockup to `docs/Design/` (`TG Inventory UI.dc.html` — 11 screens + shell + responsive states, generated from `ui-ux-design-brief.md`). This is effectively a full visual redesign of the app; split into phases to respect the WIP limit of 1-2. **Phase 0 (theme + shell + login) implemented**: a real Angular Material theme with the brief's exact palette (`mat.m2-define-palette` over CSS custom properties in `styles.scss`, replacing the generic `indigo-pink` placeholder), Phosphor icons (`@phosphor-icons/web`, replacing Material Icons — previously only the shell used any icons), the shell and login redesigned pixel-for-pixel against the mockup (exact gradients, typography, spacing). Login moved from `mat-card`/`mat-form-field` to plain hand-styled inputs, matching the mockup — a deliberate call: the mockup is now the authoritative visual source, not the original brief's generic "use Material's own patterns" guidance. **Verified**: lint/build/test green (40 tests), dev server boots without errors, confirmed the built CSS actually contains the real color tokens. **Could not be verified visually in a real browser** (no such tooling in this environment). **Closed** (2026-08-17): PR #89 merged into `staging`, Vercel preview (`tg-inventory-app-git-staging-hatori2.vercel.app`) deployed `READY` — the user still needs to review it visually there before continuing. **Phase 1 (Dashboard) implemented**: went from an empty greeting (`<h1>Welcome</h1>`, no links at all) to a real landing page — a gradient banner greeting the user by first name, a quick-access grid (icon, label, description, route), and a "no indicators yet" panel (MVP1 has no reporting story). The grid is built from `visibleSections()`, the same data source the shell now uses too (extracted into `core/layout/nav-items.ts`, previously duplicated inside `ShellComponent`) — dashboard and sidenav can never drift on what accesses they show. Also updated `frontend/public/logo-placeholder.png` (the user swapped the logo). **Real finding during this phase's TDD**: the `fakeJwt()` helper used across specs (`shell.component.spec.ts`, `auth.service.spec.ts`, now also `dashboard.component.spec.ts`) never UTF-8-encoded the payload before base64-encoding it — with an accented name ("María Cabrera") the real decoder (`decodeJwtPayload`) threw silently and `AuthService` treated the session as absent, producing false test negatives. Not a production bug (NestJS's real `JwtService` does UTF-8-encode correctly) — fixed only in `dashboard.component.spec.ts`'s copy; the other two files never hit it because they never tested an accented name, but carry the same latent fragility if someone does. **Verified**: lint/build/test green (43 tests, 3 new), dev server boots without errors. **Could not be verified visually in a real browser.** **Post-Phase-1 fix (logo/favicon)**: the user reported a broken logo after Phase 1 merged. Real cause, not caching or a filename mismatch: **`frontend/public/` was never actually wired into the build** — `angular.json`'s `assets` only copied `src/favicon.ico` (which doesn't even exist) and `src/assets`; anything placed in `public/` (including the original `logo-placeholder.png`) never made it into the built bundle, at any point in the project. Fixed by adding `{ "glob": "**/*", "input": "public" }` to `assets` in both `build` and `test`. Along the way: the user uploaded the real logo (`Logo.png`) and a favicon (`faviconTG.png`), both moved into `public/images/` (favicon included — the browser's automatic `/favicon.ico` fallback doesn't apply to a custom-named file, so there was no real reason to keep it at the root), and the logo is now a link to `https://tgconsultores.net/` (new tab) in both the shell and login. **Verified**: lint/build/test green (43 tests), confirmed with `curl` against the dev server that `/images/Logo.png` and `/images/faviconTG.png` respond `200` with the correct size. **Phase 2 (Current stock) implemented**: full visual redesign (filters, table, a "Lote" column, pagination) against the mockup's palette/typography, plus a "Clear filters" button that didn't exist before. **Real functionality added, not just visual**: real pagination — HU-10's backend already supported `page`/`pageSize`, but the frontend never exposed them (always requested page 1 at a fixed size of 100); now there's a rows-per-page selector (20/50/100, defaulting to 20 to match `PaginationQueryDto`) and previous/next buttons that respect the real bounds (`total`/`totalPages`). Changing any filter or the page size resets to page 1. Dropped the loose "Volver" link (already covered by the shell). **Verified**: lint/build/test green (50 tests, 7 new covering `totalPages` math, pagination boundaries, and that changing filters/page size resets to page 1), dev server serves `/inventory/stock` without error. **Could not be verified visually in a real browser.** **Phase 3 (Movements) implemented**: type selector as icon pills (was a plain `<select>`) and a field grid reorganized against the mockup — "Destination location" (transfer) and "Adjustment direction" (adjustment) blocks highlighted in blue when relevant. **New sidebar panel** ("What gets written"): shows live the type being sent, how many rows the movement writes to the ledger ("1" or "2 (out and in)" for transfer), and the real origin stock delta with sign and color (green/red) — unlike the mockup, which showed static example text ("+24 or -24 depending on direction"), this one derives the value from the actual form state (type + direction + quantity), since it's a working form, not a static mock. The loose "Manage batches"/"Check current stock" links that used to sit above the form moved into the sidebar as "Shortcuts" (with icons), exactly as the mockup's own note asked for. **Verified**: lint/build/test green (55 tests, 5 new covering the delta by type/direction, switching type via `selectType()`, and the submit button's label). Dev server serves `/inventory` without error. **Could not be verified visually in a real browser.** **Phase 4 (Batches) implemented**: product filter reorganized with a contextual note, table card with a header/count, inline add row at the bottom — all against the mockup's palette/typography. **Two real features added, not just visual**: (1) a "Recibido" input to backdate a batch's received date — `CreateBatchRequest.receivedAt` already existed in the frontend model, but no input ever exposed it; (2) a "Stock del lote" column — there's no endpoint returning per-batch totals, so it's computed client-side: picking a product also fetches `GET /inventory/stock` filtered by that product (already existed since HU-10) alongside `listBatches()`, summing quantities grouped by `batchId` via `forkJoin`. **Verified**: lint/build/test green (58 tests, 3 new covering the requires-batch product filter, summing stock per batch across multiple locations, and that `receivedAt` travels in the POST when filled in), dev server serves `/inventory/batches` without error. **Could not be verified visually in a real browser.** **Phase 5 (Products) implemented**: kept the existing route-based navigation (`/products` list, `/products/new`|`/products/:id/edit` form, instead of the mockup's side-by-side layout) and applied the mockup's styling to each screen separately. List: a description subline under the name, "Lote"/"Estado" badges, real pagination (same gap as Stock before phase 2 — the backend already supported `page`/`pageSize`, it was never exposed). Form: labeled fields, unit/category with a "+ Crear" button and a highlighted inline-create box, "Requiere lote" as a checkbox block, status as a pill toggle (edit mode only). **Two things from the mockup deliberately not replicated**, documented right in the code: (1) the name search box — `GET /products` has no name filter on the backend, and a box that only filtered the currently-loaded page would be misleading, not useful; (2) gating "+ Nuevo producto"/"Editar" behind permissions (the mockup's `canWriteProducts`) — the frontend doesn't track granular permissions today, only role, so they stay visible to any authenticated user and the backend still enforces the real block with `403` (unchanged behavior). **Verified**: lint/build/test green (62 tests, 4 new covering `totalPages` and pagination boundaries for products), dev server serves `/products` and `/products/new` without error. **Could not be verified visually in a real browser.** **Closed**: PR #97 merged into `staging` (confirmed via the GitHub API, `merged:true`). **Phase 6 (Categories/Units) implemented**: both screens moved from an unstyled table (bare `<h1>` + inline form + plain `<table>`) to the mockup's "catalog" pattern — a card with a header and count (`"N categorías"`/`"N unidades"`), Name/Products/Status/action columns, an inline add row at the bottom of the table (instead of a loose form above it), and a footer note ("Deactivating doesn't delete..."). **Real gap resolved, not just visual**: the mockup's "Productos" column has no backend support — neither `Category` nor `Unit` exposes a count, and no endpoint computes one — so it's computed client-side: alongside the catalog itself, `GET /products` is fetched (up to 100, this project's current volume) via `forkJoin` and counted by `categoryId`/`unitId`, the same pattern Phase 4 used for per-batch stock. **Verified**: lint/build/test green (68 tests, 6 new — 3 per component covering the product count, adding an item with `trim()`, and the status toggle), `ng lint` clean, dev server not verified visually in a real browser. **Remaining phases**: Locations, Users, Roles and permissions. Each as its own PR.

> **Post-TT24-closure — user's first real visual review on the Vercel preview (2026-08-18)**: found the spacing between sidenav sections (CATÁLOGO/INVENTARIO/ADMINISTRACIÓN) too tight. First attempt (PR #102, merged into `staging`) turned out to be a no-op: the flex `gap` never actually applied because `MatDrawer` wraps its content in its own `mat-drawer-inner-container`, two levels deeper than where `.shell-sidenav` lived. **Real fix** (branch `fix/shell-nav-section-spacing-v2`): `margin-top: 24px` on `.shell-nav-section-label`, confirmed in the compiled CSS and verified visually by the user after a hard cache clear. In the same session, the user also reported errors in VSCode's Problems panel (the ESLint extension flagged every `.ts` file with a parsing error) — real cause: `parserOptions.project` in both `.eslintrc.js` files resolved relative to the linting process's cwd, which for the VSCode extension is the repo root, not `backend/`/`frontend/` where each config actually lives. Fixed by adding `tsconfigRootDir: __dirname` to both — user confirmed the panel came back clean. **Verified**: `npm run lint` green in both projects (backend 0 errors/47 pre-existing unrelated warnings, frontend no findings). **Closed**: PR #103 merged into `staging` (2026-08-19T01:54:53Z) — this closes TT-24 completely, including the fix from the user's visual review.

> **Sync `staging` → `main` (2026-08-19)**: since `main` had fallen behind since the last sync (PR #80/#83) — missing all 10 TT-24 phases, real logout (ADR-32/TT-27), and both PR #103 fixes —, opened and merged **PR #104** (`sync/staging-to-main-tt24-shell-fixes`), same pattern as ADR-30: a dedicated branch off `main`, merging `staging` into it. Only real conflict: `docs/esp/decisiones.md`/`docs/en/decisions.en.md` (`main` already had ADR-31 backported via PR #86 but not ADR-32) — resolved by keeping staging's ADR-32 entry, no code conflicts. Verified locally before committing the merge: backend `lint`/`build` green, 60 suites/240 tests; frontend `lint`/`build` green, 96/96 tests.

> **TT-27 — Real logout (ADR-32) — ✅ Closed.** The code was already merged into `staging` before this session (confirmed: logout code arrived as part of merging `staging` into the PR #104 sync); what was still missing was running `npm run prisma:migrate:staging` for migration `20260817050000_add_revoked_token` — **it had never actually been run**, despite being flagged as pending both in this file and on the Trello card itself. **Real production incident found (2026-08-19)**: the user reported `POST /roles` → `500` while testing the app right after the `main` sync. Diagnosed via real Render logs (`list_logs`, MCP): `PrismaClientKnownRequestError: The table "public.revoked_token" does not exist`, thrown from `JwtStrategy.validate()` — since **production and staging share the same Neon DB and the same Render service** (see ADR-30), this broke **every authenticated endpoint**, not just `/roles`, for any user whose JWT already carried a `jti` (any login issued after the logout code merged). Fixed by running `npm run prisma:migrate:staging` against the real DB — it applied two pending migrations: `20260815000000_add_location_name_unique_per_parent` (from HU-06, also never run) and `20260817050000_add_revoked_token`. **Verified**: `/health` → `200 OK` before and after; Render logs show no new errors from the migration onward (`list_logs` filtered by `level:error` from before the fix through ~1h later, empty). **Still needs user confirmation**: retry the action that returned `500` (creating a role) in the browser to close the loop — the user moved on to MVP2 instructions before explicitly confirming it in this session.

Keep this updated yourself as each iteration closes.

## Structure note

`docs/` is organized into subfolders — `docs/esp/` (Spanish), `docs/en/` (English), `docs/Diagrams/` (all PNGs). See ADR-16 in `decisions.en.md`.

## What this project is

Inventory control system: suppliers, inventory by location, stock alerts, purchase history, price comparison, RBAC, and internal requests with an approval flow. A single developer, weekends only. See `docs/en/PRD.en.md` for full detail.

## Where things stand right now

**Phase**: Iteration 1 (HU-01→02→03→28) — complete. **Iteration 2 (HU-06→07→08→09→10) — complete end to end**, all merged into `staging` and verified (PR #70, #72, #74, #76, #78). **`main` synced with `staging`** (2026-08-17 via ADR-30, and again 2026-08-19 via PR #104 — full TT-24 + real logout/TT-27). **MVP1 and its UAT round are fully closed** with TT-24 and TT-27 closed out. **MVP2 starts** (2026-08-19): HU-04 → HU-13 → HU-05 → HU-14, agreed order — HU-04 first (a real supplier before anything can buy from one).

> **Note**: the detailed HU table below (English mirror) was last fully updated through HU-28 and has not been kept in lockstep with the Spanish section above, which is current through MVP2 kickoff. For the authoritative, up-to-date detail on HU-06 through HU-10, the production incidents, and MVP2 progress, read the Spanish section at the top of this file (or `docs/en/decisions.en.md`, kept current in English for ADRs).

### Immediate next step: decide with the user a real staging/production isolation strategy (Neon branching, a second Render service, etc.) before UAT — see ADR-30 and the persisted memory note on this topic. Once resolved, the next milestone is UAT with the business user (see master plan)

TT-02 through TT-05 are now closed (see table below). All nine technical tasks from the architecture review (see "Architecture gaps" below) are closed — merged into `staging` and, where applicable (TT-17/TT-18), with their migration already applied to Neon staging.

**TT-23 — Parametrized catalogs (Category, Unit)** — ✅ Closed (2026-08-14), merged into `staging` (PR #61). At the user's request, validated whether the schema lets a role administer "catalog tables" from the frontend. Finding: `Role`/`Permission` (HU-02) and `ApprovalFlow` (ADR-08) were already real parametrized tables; `Product.category`/`Product.unit` were free `String` fields with no dedicated table. Solution: `Category`/`Unit` added as tables (same pattern as `Role`), `Product.categoryId`/`unitId` as FKs — see **ADR-23**. Migration `20260814045501_add_category_unit_catalogs` applied locally and also to Neon staging (`npm run prisma:migrate:staging`) — verified with `curl` against the real Render backend after the migration: `/health` → `200 OK`, `database: ok`. Endpoints (`/categories`, `/units`) documented in the plan; controller/service implementation deferred to whenever HU-28 gets implemented, whose text was already adjusted to reflect the new design (catalog selects, not free text).

The PR + CI flow itself **works and has been verified end to end** (see TT-07 detail below), and is now backed by the branch protection rule too.

### Architecture gaps (TT-14 through TT-21)

Origin: an architecture review session (2026-08-12), triggered by a concrete question — "if one module fails, do the others go down with it?". The design is a modular monolith (every module in a single Node process, one shared `schema.prisma`/`PrismaClient`) — without explicit discipline, the answer is yes. Eight TTs (not HUs — cross-cutting technical prerequisites) address this and related gaps (concurrency, idempotency, pagination, indexes, logging, module boundaries), before any real business story gets implemented. Full detail on each in Trello, "Current iteration" list.

- **TT-14 (module boundaries)** — ✅ Done. See row in the table below.
- **TT-15 (global error handling)** — ✅ Done. See row in the table below.
- **TT-16 (Prisma connection limits)** — ✅ Done. See row in the table below.
- **TT-21 (logging)** — ✅ Done. See row in the table below.
- **TT-19 (pagination)** — ✅ Done. See row in the table below.
- **TT-20 (indexes)** — ✅ Done. See row in the table below.
- **TT-17 (optimistic locking on stock)** — ✅ Done. See row in the table below.
- **TT-18 (idempotency)** — ✅ Done. See row in the table below.

**TT-22 — Soft delete** (new, not part of the original eight): a finding from TT-20 — no `DELETE` is planned on any API endpoint, and 13 of 17 models have neither `status` nor `deletedAt`. Decision made and documented as **ADR-22**: `deletedAt DateTime?` gets added model by model, only once the first real story defines a "delete" action on that specific model — never preemptively across all 17 at once — with reads filtered via a Prisma Client extension. No model is touched yet (no story consumes it) — the decision itself is already merged into `staging`.

### Technical tasks, in detail

| Technical task | Status |
|---|---|
| TT-01 (create GitHub repo) | ✅ Done — push confirmed |
| TT-11 (health-check) | ✅ Done — re-validated |
| TT-12 (Prisma + migration) | ✅ Done — re-validated |
| TT-13 (local Docker Postgres) | ✅ Done — re-validated |
| TT-07 (CI) | ✅ Done — verified with real PRs (#13, #14) against `staging`, both checks (`backend`, `frontend`) green. See "What was found and fixed" below. **Refinement (ADR-19)**: the checks had `paths:` on the trigger, so on docs-only PRs (several this session) the required check never fired and branch protection waited forever — forcing a bypass. Dropped `paths:` from the trigger and added `dorny/paths-filter` to gate the real work per step; now a docs-only PR passes in seconds without a bypass, and one that does touch `backend/`/`frontend/` still runs the full pipeline as before |
| TT-02 (branching + branch protection) | ✅ Done — repo switched to public; two GitHub rulesets created (`main` and `staging`), Enforcement status `Active` on both, with `Require status checks to pass` requiring `backend` and `frontend`. Trello status still needs manual sync (no connector from this environment) |
| TT-03 (Vercel) | ✅ Done — project connected to the repo (auto-deploy on push to `main` confirmed); after fixing the Framework Preset (`Angular`) and Output Directory (`dist/frontend/browser`, required by Angular 18's `application` builder), `https://tg-inventory-app.vercel.app` responds `200 OK` serving `index.html` |
| TT-04 (Render/Fly.io) | ✅ Done — `tg-inventory-backend` service created on Render (`https://tg-inventory-backend.onrender.com`), connected to the repo; `DATABASE_URL`, `JWT_SECRET`, `JWT_EXPIRES_IN`, `NODE_ENV`, `FRONTEND_URL` all set. Verified with `curl`: `/health` → `200 OK` (`database: ok`), and CORS (GET + preflight OPTIONS) returns `access-control-allow-origin: https://tg-inventory-app.vercel.app` |
| TT-05 (Neon/Supabase) | ✅ Done — database created on Neon, migrations applied with `npm run prisma:migrate:staging`, `DATABASE_URL` already copied into Render's secrets (TT-04) |
| TT-06 (Cloudflare) | ✅ Done (2026-08-14, 5/5 items, confirmed by the user) — account created at cloudflare.com; WAF/proxy on hold (no custom domain added yet, using Vercel/Render's free subdomains — not blocking); R2 bucket created; API token generated with permissions scoped to that bucket; credentials (`R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`) saved locally only for now — correct, R2 isn't consumed by any code until MVP 5 (HU-26/27), loaded as a real secret in Render once that time comes |
| TT-08 (CD) | ✅ Done (2026-08-14, 6/6 items) — **NOT a workflow**, it's configuration inside the Vercel/Render dashboards. Real `apiUrl` in `environment.prod.ts`/`environment.staging.ts` merged (PR #32); Vercel Production Branch = `main` (confirmed by the user in the dashboard); Vercel auto-generates a preview/branch deploy for `staging` and every branch (confirmed with real deployment-history data, alias `tg-inventory-app-git-staging-*`); Render auto-deploy on "On Commit" pointing to `main` (confirmed by the user); branch protection active on `main`/`staging` (TT-02). Second Render service for "staging" **deliberately postponed** (decision 2026-08-14) — a single service is enough while there's no real staging/production split driven by a business story |
| TT-09 (Dependabot) | ✅ Done (2026-08-14, 4/4 items) — `dependabot.yml` groups by ecosystem. "Dependabot version updates" confirmed with real evidence (PR #28 from `dependabot[bot]`); `npm audit` gate in CI already verified in TT-07; "Dependabot alerts" and "Dependabot security updates" enabled by the user in GitHub → Settings → Code security |
| TT-10 (secrets) | ✅ Done, verified item by item (2026-08-14) — `DATABASE_URL`, `JWT_SECRET`, `JWT_EXPIRES_IN`, `NODE_ENV`, `FRONTEND_URL` loaded on Render (never in the repo, see `.env.example`). **GitHub Actions needs no secrets**: `ci-backend.yml`/`ci-frontend.yml` use no `secrets.*` and touch no real DB or the deployed API (only `lint`/unit `test`/`audit`/`build`). **Vercel needs no secrets today either**: `apiUrl` is a hardcoded public URL, not a credential. **Security check run now**: `git log --all --full-history -- backend/.env` and `-- backend/.env.staging` both return empty — both files correctly in `.gitignore`, a real `.env` was never committed. Revisit if a real secret is ever needed in GitHub Actions or Vercel |
| TT-14 (module boundaries) | ✅ Done — ADR-18 documented; `eslint-plugin-boundaries` in `backend/.eslintrc.js` fails `npm run lint` (and therefore `ci-backend.yml`) if a module imports another module's `domain/`/`infrastructure/`. Verified with a throwaway fixture (same-module import passes, cross-module fails with the ADR-18 message) — fixture discarded after confirming |
| TT-15 (global error handling) | ✅ Done — `GlobalExceptionFilter` (`backend/src/common/filters/`) registered via `APP_FILTER`: any exception (known or not) responds with a consistent shape (`statusCode`, `message`, `timestamp`, `path`); unknown errors return a generic 500 without leaking internal details, logged server-side. `process.on('uncaughtException'/'unhandledRejection')` in `main.ts` log and shut down in a controlled way (Render restarts the container). Verified with 3 unit tests (`global-exception.filter.spec.ts`) and a live check: compiled server run locally, `/health` → `200`, a nonexistent route → `404` in the filter's format |
| TT-16 (Prisma connection limits) | ✅ Done, including the real variable on Render — `connection_limit=5`, `pool_timeout=10`, `connect_timeout=10`, `options=-c statement_timeout=10000` documented in `.env.example`/`TRD.en.md` and already applied to Render's `DATABASE_URL`. Verified empirically against local Postgres (a 2s query passes, a 15s query gets cancelled at ~10s with Postgres' real `57014` error) and live: `curl` to production `/health` → `200 OK`, `database: ok`. Neon's numbers (104 connections at 0.25 CU, 97 after reserving 7 for the superuser) confirmed against its official docs. **Incident during rollout**: the first attempt on Render failed with `P1001 (Can't reach database server)` — the real cause was a stray trailing `"` (and likely a leading one too) carried over from the `DATABASE_URL="..."` format used in `.env` files when the value was pasted into Render's field (Render doesn't do dotenv-style parsing — it takes the pasted value verbatim). Reproduced locally to confirm before pointing to the fix. **Gotcha to remember** when copying any other value from `.env.example`/`.env.staging` into Render's dashboard: paste only the content between the quotes, never the quotes themselves |
| TT-21 (structured logging) | ✅ Done — `nestjs-pino` (`backend/src/config/logger.config.ts`, loaded in `app.module.ts`, `app.useLogger()` in `main.ts` with `bufferLogs: true`). Structured JSON to stdout in `staging`/`production`, pretty-print in `development`. Correlation id per request (`X-Request-Id`, propagated if the client already sends one). Automatic redaction of `Authorization`, cookies, `password`/`token` in the body — never in plaintext. Convention documented in `conventions.en.md` (what to log: errors/5xx always, explicit critical writes, requests already covered by `pino-http`). Verified live with the compiled server in both modes: raw JSON in `production`, pretty-print in `development`, `Authorization` header with a real token confirmed as `[Redacted]` in the log. `npm run lint`/`build`/`test` green |
| TT-19 (standard pagination) | ✅ Done — offset/limit convention for every future listing endpoint (HU-05/08/10), documented in `TRD.en.md` section 4 and `conventions.en.md`. Shared implementation in `backend/src/common/`: `dto/pagination-query.dto.ts` (`page`/`pageSize`, validated with `class-validator`, `pageSize` capped at 100 to avoid TT-16's "noisy neighbor"), `dto/paginated-response.dto.ts` (`{ items, total, page, pageSize }` shape), `utils/pagination.util.ts` (`toPrismaSkipTake()`, `buildPaginatedResponse()`). 8 new unit tests (defaults, boundary validation, `skip`/`take` math). Along the way, fixed a real gap in `jest.config.js` — `setupFiles: ['reflect-metadata']` was missing, without it any DTO using `class-transformer`'s `@Type()` failed in tests with `Reflect.getMetadata is not a function`. `npm run lint`/`build`/`test` green (12 tests, full suite) |
| TT-20 (DB indexes) | ✅ Done — verified empirically that PostgreSQL doesn't auto-index FKs (unlike MySQL): none of the 27 FK columns in the initial migration had an index. 17 new indexes added with the criteria documented in `TRD.en.md` section 3 (every FK used for filtering/joins, except very small config tables; composites where the query pattern is composite — `InventoryMovement[productId, locationId]`, `Purchase[supplierId, purchasedAt]`, `AuditEvent[entity, entityId]`; no duplicating what a `@@unique` already covers). Migration `20260813043614_add_indexes` generated and applied locally (`prisma migrate dev`), SQL reviewed line by line (only `CREATE INDEX`, nothing else). Verified live: compiled server up, `/health` → `200 OK`. Also applied to Neon staging with `npm run prisma:migrate:staging` (`migrate deploy`, ADR-15's mechanism) — verified with `curl` against the real Render backend after the migration: `/health` → `200 OK`, `database: ok`, no downtime. **Separate finding, out of scope**: no soft-delete (`deletedAt`) and no `DELETE` planned on any API endpoint — 13 of 17 models don't even have the `status` pattern `User`/`Location`/`Product`/`Supplier` use. Nothing touched about it; left for a new TT the user will add to Trello |
| TT-17 (optimistic locking on stock) | ✅ Done — merged into `staging` (PR #44). `version Int @default(0)` column on `LocationStock` (migration `20260813180448_add_location_stock_version`, a single `ALTER TABLE ADD COLUMN`, reviewed line by line), reusable `withOptimisticLock()` utility (`backend/src/common/utils/optimistic-lock.util.ts`): retries up to 3 times on a version conflict, then throws `409 Conflict`. Decision documented in **ADR-20** (discards `SELECT FOR UPDATE` and 409-with-no-retry). Covered with 4 TDD unit tests. No real consumer yet (Iteration 0, no movements use-case implemented) — ready for HU-08/HU-16/HU-17. Migration also applied to Neon staging (`npm run prisma:migrate:staging`) — verified with `curl` against the real Render backend after the migration: `/health` → `200 OK`, `database: ok` |
| TT-18 (idempotency) | ✅ Done — merged into `staging` (PR #45). `IdempotencyKey` table (unique `key`, `endpoint`, `response` as `Json`, `createdAt`; migration `20260813181409_add_idempotency_key`, a single `CREATE TABLE` + unique index), reusable `@Idempotent()` decorator + `IdempotencyInterceptor` (`backend/src/common/`): requires the `Idempotency-Key` header, returns the already-stored response without re-running the handler when the key repeats, and resolves the race between two concurrent requests sharing a key via Postgres' `P2002` code (unique constraint), returning whichever response won instead of failing. Decision documented in **ADR-21** (discards Redis, disabling the frontend button). Covered with 5 unit tests (mocked `PrismaService`/`Reflector`/`ExecutionContext`, including the race case). No real consumer yet; ready for HU-08/HU-13/HU-15/HU-16. Migration also applied to Neon staging together with TT-17's — verified with `curl` against the real Render backend: `/health` → `200 OK`, `database: ok` |
| TT-22 (soft delete: strategy and scope) | ✅ Done — merged into `staging` (PR #46). **ADR-22**: `deletedAt DateTime?` gets added model by model, only once the first real story defines a "delete" action on a model without an existing `status` (`User`/`Location`/`Product`/`Supplier` untouched); reads filtered via a Prisma Client extension (`$extends`), not a hand-repeated `where: { deletedAt: null }`. Never applies to `InventoryMovement`/`AuditEvent` (historical record, ADR-07) nor to link/config tables or derived rows with no lifecycle of their own (`RolePermission`, `ApprovalFlow`, `LocationStock`, `PurchaseItem`, `RequestItem`). No code or schema changes — this is exactly what the Trello card itself asks for (apply the pattern only once the first story needs it, not preemptively) |

### User stories (HU), in detail

| HU | Status |
|---|---|
| HU-01 (user login) | ✅ Merged into `staging` (PR #65, branch `feature/hu-01-login` deleted locally and remotely after the merge). Backend: new `auth` module (`backend/src/modules/auth/`, hexagonal architecture — `domain/auth-user.entity.ts` with `isActive()`, `application/use-cases/login.use-case.ts`, `infrastructure/user.prisma.repository.ts` + `jwt.strategy.ts`), `POST /auth/login` (the only endpoint, `@Public()`), the same generic error message for a missing user/wrong password/blocked user (avoids enumeration). `JwtAuthGuard`/`RolesGuard` are now registered **globally** via `APP_GUARD` (previously unwired stubs); new `@Public()` decorator (`backend/src/common/decorators/public.decorator.ts`) used on `POST /auth/login` and on `GET /health` (otherwise `/health` would have broken Render/CD monitoring). Full decision in **ADR-24**. New seed (`backend/prisma/seed.ts`, idempotent, requires `SEED_ADMIN_EMAIL`/`SEED_ADMIN_PASSWORD` from the environment — never generates or logs a password), `prisma:seed`/`prisma:seed:staging` scripts added. No new schema migration (the `User`/`Role` models already existed). Tooling adjustments needed to make this work: `backend/jest.config.js` extended to discover `test/bdd/` (previously only saw `src/`), new `backend/tsconfig.build.json` + `nest-cli.json` pointing to it (separates the build — which excludes `test/`/`prisma/` — from the project ESLint/`tsconfig.json` uses, which does need to see them) — without this, `nest build`/`nest start` failed with `TS6059` on hitting a file outside `rootDir`. Frontend: new `AuthService` (`core/services/`, `user` signal, session restored by decoding the JWT from `localStorage`, no `/auth/me` endpoint), `jwt.util.ts` (decodes the payload without installing `jwt-decode`), `login.component.ts` actually wired up (previously `onSubmit()` was empty), `role.guard.ts` fixed — **real security finding**: it previously didn't check authentication at all, anyone without logging in could reach `/suppliers`; it now requires `isAuthenticated()` first. New `/dashboard` placeholder screen (post-login destination for every role, no per-role screens exist yet). **Verified**: backend — `npm run lint`/`build`/`test` green (11 test suites, 35 tests: TDD for the use-case/entity/controller/guard + BDD for 4 login scenarios via `jest-cucumber`+`supertest` against an in-memory fake repository); frontend — `npm run lint`/`build`/`ng test --browsers=ChromeHeadless` green (12 tests). **Real manual verification, local** (backend run with `start:dev`, seed run with test credentials `admin@tg-group.local`/`Verification-Test-123`): `GET /health` → `200` with no token; `POST /auth/login` with correct credentials → `200` + `accessToken` + `user` with role; with a wrong password and with a nonexistent user → `401` with the same generic message in both cases. Frontend (`ng serve`) compiled and served `/login` with `200`, lazy chunks for `login-component`/`dashboard-component` present. **Could not be verified visually in a real browser** (no `chromium-cli`/Playwright available in this Windows environment without an unrequested new install) — the UI was tested via HTTP/build/unit tests, not a screenshot. **Closed**: PR merged into `staging`; verification on Neon staging/Render with `npm run prisma:seed:staging` is left to the user (requires their own `SEED_ADMIN_PASSWORD` in `.env.staging`), not done from this environment. |
| HU-02 (create roles and assign permissions) | ✅ Merged into `staging` (PR #66). Backend: new `roles` module (`backend/src/modules/roles/`, hexagonal architecture — `domain/role.entity.ts` with `hasPermission(module, action)`, `domain/permission.value-object.ts` (self-validating Value Object), 4 use-cases, `infrastructure/role.prisma.repository.ts` + `permission.prisma.repository.ts`), endpoints `GET/POST /roles`, `PATCH /roles/:id` (replaces the full permission set, transactional), `GET /permissions` (not in the master plan's section 7.4 table, added so the frontend can build the assignment form). New **`PermissionsGuard`** (`backend/src/common/guards/permissions.guard.ts`) + `@RequirePermission(module, action)` decorator — distinct from `RolesGuard` (HU-01, compares role name): this one queries `RolePermission` on every marked request, registered `APP_GUARD` inside `roles.module.ts` (same pattern as `auth.module.ts`). Full decision in **ADR-25**. Additive migration `20260814230000_add_role_permission_unique_constraints` (`Role.name` and `Permission[module,action]` now `@unique`) — **hand-written** (`prisma migrate dev` doesn't run in this tooling environment, no interactive TTY available; the SQL was written directly following the exact pattern of prior project migrations like `add_category_unit_catalogs`, applied with `prisma migrate deploy`, confirmed drift-free with `prisma migrate status`). Chicken-and-egg bootstrap: `seed.ts` extended to seed 3 permissions (`roles:read/create/update`) and grant them to the "Administrador" role — **anyone who already ran HU-01's seed must re-run it** (`npm run prisma:seed`), confirmed locally (the admin seeded in HU-01 went from 0 to 3 permissions after re-seeding). Minor refactor of HU-01 code: `isUniqueConstraintViolation()` extracted from `idempotency.interceptor.ts` into `common/utils/prisma-error.util.ts` (reused by `CreateRoleUseCase`); the BDD's `FakeAuthUserRepository` extracted into `test/bdd/support/` (reused by the new BDD suite). Frontend: `role.model.ts`/`permission.model.ts`/`paginated-response.model.ts` models, `RolesService` (feature-scoped, `features/users-roles/`), 3 components (`roles-list`, `role-form`, `role-permissions` with checkboxes), `/roles` route protected by the existing `roleGuard` (`data: { roles: ['Administrador'] }` — no granular permission system on the frontend this round, an explicit ADR-25 decision). **Verified**: backend — `npm run lint`/`build`/`test` green (22 test suites, 72 tests: TDD for the new domain/use-cases/guard/controllers + BDD for 3 scenarios in `manage-roles.feature`, including the story's literal criterion — a user without the `roles:create` permission → `403`); frontend — `npm run lint`/`build`/`ng test --browsers=ChromeHeadless` green (16 tests). **Real manual verification, local** (backend run with `start:dev`): admin login → `POST /roles` creates a role → `201`; same name again → `409`; `GET /roles`/`GET /permissions` list paginated; `PATCH /roles/:id` assigns a permission → `200`; `PATCH` with a nonexistent `permissionId` → `400`; `PATCH` on a nonexistent role → `404`; no token → `401`; a user with the "Comprador" role (no `roles:create`, created ad hoc for the test) attempts to create a role → **`403`**, confirming the story's exact acceptance criterion, and the same user can `GET /roles` (has `roles:read`) → `200`. Frontend (`ng serve` — a stale server on the same port was found and killed before repeating the test, to avoid a false positive) built cleanly with the new chunks (`roles-list-component`, `role-form-component`, `role-permissions-component`, `users-roles-routes`). **Could not be verified visually in a real browser** (same environment limitation as HU-01, no `chromium-cli`/Playwright). **Closed**: PR merged into `staging`; migration and seed on Neon staging are left to the user, not done from this environment. |
| HU-03 (assign a role to a user) | ✅ Merged into `staging` (PR #67). Backend: new `users` module (`backend/src/modules/users/`, hexagonal architecture — `domain/user.entity.ts` with `isActive()`, 4 use-cases including an exported `ValidateUserCredentialsUseCase`, `infrastructure/user.prisma.repository.ts`), endpoints `GET/POST /users`, `PATCH /users/:id` (edits name/email/roleId/status — **never password**, no reset endpoint yet, explicit ADR-26 decision). **`auth` refactor** (resolves technical debt HU-01 explicitly flagged): `auth` no longer has its own `User` repository — `auth/domain/auth-user.entity.ts`(+spec)/`auth-user.repository.interface.ts`/`auth/infrastructure/user.prisma.repository.ts` deleted, `login.use-case.ts` rewritten to consume `ValidateUserCredentialsUseCase` (exported by `UsersModule`), which does `bcrypt.compare`+`isActive()` internally and never lets the `User` entity or its `passwordHash` out — good side effect: the generic invalid-credentials message is now structurally guaranteed (login can no longer distinguish the 3 causes). `AuthenticatedUserDto` moved from `auth/dto/` to `users/dto/`. New `isForeignKeyViolation()` (P2003) in `common/utils/prisma-error.util.ts`, alongside the already-existing `isUniqueConstraintViolation()` (P2002) from HU-02 — validates that `roleId` really exists without `users` querying the `roles` module (ADR-18). Bootstrap: `seed.ts` extended with `users:read/create/update` granted to Administrador — anyone who already ran HU-02's seed must re-run it. Full decision in **ADR-26**. No new schema migration (the `User` model already existed complete). Necessary migration of existing BDD suites: `login.steps.ts` and `manage-roles.steps.ts` (both depended on `AUTH_USER_REPOSITORY`, now deleted) updated to the new `USER_REPOSITORY`/`FakeUserRepository` (`test/bdd/support/`, replacing `fake-auth-user.repository.ts`) — confirmed `overrideProvider` from `@nestjs/testing` still replaces the token across the whole module tree regardless of how many modules redeclare it. Frontend: `user-account.model.ts` (distinct name from `user.model.ts`, HU-01's session model), `UsersService`, routes split into `ROLES_ROUTES`/`USERS_ROUTES` inside `users-roles.routes.ts` (previously a single `USERS_ROLES_ROUTES` with only roles), `app.routes.ts` with two entries (`/roles`, `/users`). `users-list` and `user-form` components (one component, two modes: create asks for a password, edit doesn't — edit resolves the user by filtering the paginated `listUsers()`, same criterion as HU-02's `role-permissions.component.ts`, no `GET /users/:id` in the master plan). **Verified**: backend — `npm run lint`/`build`/`test` green (28 test suites, 94 tests: TDD for the new domain/use-cases/controller + a rewritten `login.use-case.spec.ts` + a new BDD suite with 4 scenarios in `manage-users.feature`, including the story's literal criterion — reassigning a role is reflected on the next login); frontend — `npm run lint`/`build`/`ng test --browsers=ChromeHeadless` green (19 tests). **Real manual verification, local** (backend run with `start:dev`): admin login still works after the refactor; `POST /users` creates a user with role "Comprador" → `201`; duplicate email → `409`; nonexistent `roleId` → `400`; no token → `401`; logging in as the newly created user → reflects role "Comprador" in the JWT; admin reassigns the user to role "Administrador" via `PATCH /users/:id` → `200`; `PATCH` with no fields → `400`; **logging in as the same user again → the JWT now carries role "Administrador"**, confirming the story's exact acceptance criterion live. Frontend (`ng serve` — a stale server was found and killed before repeating the test) built cleanly with the new chunks (`users-list-component`, `user-form-component`). **Could not be verified visually in a real browser** (same environment limitation as HU-01/HU-02, no `chromium-cli`/Playwright). **Closed**: PR merged into `staging`; seed on Neon staging is left to the user, not done from this environment. |
| HU-28 (create and edit catalog products) | 🟡 Implemented and verified **locally**, PR still to be opened against `staging` (branch `feature/hu-28-catalog-products`), **not merged yet**. Last story in this batch. Backend: new `products` module (`backend/src/modules/products/`) grouping `ProductsController`+`CategoriesController`+`UnitsController` — same grouping criterion as `RolesModule`+`PermissionsController` (HU-02), since `Category`/`Unit` only exist for `Product` to reference. **First deliberate use of `conventions.en.md`'s carve-out** ("trivial CRUD can start without the four layers"): **no `domain/` layer** — the three repositories (`ProductPrismaRepository`/`CategoryPrismaRepository`/`UnitPrismaRepository`) are concrete classes injected directly, no port interface or `Symbol`. Endpoints: `GET/POST /products`, `PATCH /products/:id` (incl. `status: discontinued` for logical "deletion", ADR-22 — no `requiresBatch`/`imageUrl`, out of scope for HU-09/HU-26/27); same pattern for `/categories` and `/units` (with `status: active/inactive`). **Deliberate deviation from the HU-02/03 pattern**: `GET` endpoints carry **no** `@RequirePermission()` — they're "any authenticated user" per the master plan's literal spec (section 7.4), unlike `GET /roles`/`GET /users` which did require a permission. Only `POST`/`PATCH` carry `@RequirePermission('products'|'categories'|'units', 'create'|'update')`. Reuses the already-existing `isForeignKeyViolation()`/`isUniqueConstraintViolation()` (HU-03/HU-02) to validate `unitId`/`categoryId` and duplicate catalog names. Bootstrap: `seed.ts` extended with `products:create/update`, `categories:create/update`, `units:create/update` (**no** `:read`, since the GETs don't need it — it would sit inert) granted to Administrador. No new migration (`Category.name`/`Unit.name` already had `@unique` from TT-23). **No new ADR** — every decision in this HU was already covered by ADR-17 (hexagonal only where real logic exists), ADR-18 (module boundaries), ADR-22 (logical deletion via `status`) and ADR-25 (permission bootstrap pattern); nothing new to document. Frontend: `features/products/` folder (already reserved in `conventions.en.md`), `ProductsService` separate from `CatalogService` (Category+Unit together, same grouping criterion as the backend), `products-list`/`product-form` components (create+edit, with unit/category selects offering an inline "+ Create new unit/category" flow — no modal, no new dependency — for the common case of adding a catalog value while creating a product, exactly what the HU's own UX note asked for) and `categories-list`/`units-list` (list + inline create + activate/deactivate per row — the only real entry point for `PATCH /categories|units/:id`). Routes `/products`, `/categories`, `/units` with no `data.roles` (the "Admin Inventario" role isn't seeded, and GET is "any authenticated" anyway). Minor adjustment to `product.model.ts`/`category.model.ts` (already existing from before HU-01): `description`/`category` go from optional to `| null`, so the type is honest about what the backend actually returns. **Verified**: backend — `npm run lint`/`build`/`test` green (40 test suites, 127 tests: TDD for the 9 new use-cases + 3 controllers + a new BDD suite with 3 scenarios in `manage-products-catalog.feature`); frontend — `npm run lint`/`build`/`ng test --browsers=ChromeHeadless` green (28 tests). **Real manual verification, local** (backend run with `start:dev`): admin creates unit "Kilogramo" → `201`; creates category "Alimentos" → `201`; creates product "Arroz" referencing both → `201` with full nested objects; nonexistent `unitId` → `400`; duplicate category → `409`; `PATCH /products/:id` with `status: discontinued` → `200`; the "Comprador" user (no `products:create`, reused from earlier verification) attempts to create a product → **`403`**, but the same user **can `GET /products`** → `200`, confirming live the deliberate "any authenticated" deviation for reads. Frontend (`ng serve` — a stale server was found and killed before repeating the test) built cleanly with the new chunks (`products-list-component`, `product-form-component`, `categories-list-component`, `units-list-component`). **Could not be verified visually in a real browser** (same environment limitation as the three prior stories, no `chromium-cli`/Playwright). **Left to close the HU**: open the PR and merge it, `npm run prisma:seed:staging` on Neon (no new migration), verify against the real Render backend. Merging this HU closes the full batch (HU-01→02→03→28). |

### What was found and fixed while verifying CI (TT-07)

CI had never actually run before this — the test PR (#13) surfaced several real bugs in the skeleton, now fixed:
- **Backend**: `npm audit --audit-level=critical` failed on a real critical vulnerability in `node-tar`, reachable via `bcrypt` (a production dependency) → `@mapbox/node-pre-gyp` → `tar`. Bumped `bcrypt` to `6.0.0` (no call sites use it yet, so zero real risk). The audit gate (HU-24) now uses `--omit=dev` in both workflows so devDependency-only vulnerabilities (build tooling like webpack/Angular CLI) don't block merges.
- **Frontend**: `package-lock.json` was out of sync with `package.json` (`npm ci` rejected it) — regenerated. Neither `.eslintrc.js` nor the `lint` target in `angular.json` existed — `ng lint` had nothing to run. Zero `.spec.ts` files existed — the Tests step failed on empty config. Added `.eslintrc.js`, the lint target, and a baseline `AppComponent` spec.
- **Both**: `tsconfig.json` had no explicit `rootDir` (deprecated in TS6) and the backend had an unused `baseUrl` (deprecated in TS7) — fixed. Both workflows' job was named `test`, so GitHub reported two indistinguishable-by-name checks — given explicit names (`backend` / `frontend`) so branch protection can require them separately once configured.

Verified with `npm run lint`, `npm run build`, and `npm run test` locally in both projects, plus the real GitHub Actions checks on PRs #13 and #14.

### Story backlog (30, in the `Backlog` column)

Consistent base format across all 30 (role/action/benefit). ~10 still lack a verifiable acceptance criterion, only context notes — **not blocking**: gets completed when each one hits Definition of Ready, right before moving to "In progress" (see `docs/en/workflow.en.md`). No need to resolve this now.

**What already exists in code, already pushed to GitHub**:
- NestJS backend: hexagonal structure, full `schema.prisma` (19 entities, in English), RBAC guards/decorators, health-check
- Angular frontend: feature-based structure (all in English), `suppliers` feature as the reference template, now with working lint and a baseline spec
- `docker-compose.yml`, CI workflows (verified and green), `dependabot.yml` (grouped by ecosystem)
- `frontend/public/images/Logo.png` — real logo (replaced the placeholder, TT-24 phase 1)

## What to read, and in what order, if picking this up in a new conversation

1. **This file**
2. `docs/en/PRD.en.md` — what's being built and why
3. `docs/en/project-plan.en.md` — the master document
4. `docs/en/TRD.en.md` — technical summary
5. `docs/en/conventions.en.md` and `docs/en/decisions.en.md` — read before touching code

## Trello board

https://trello.com/b/BS5tzENy/sistema-de-control-de-inventario — TT-01 through TT-10 (original batch), TT-14 through TT-22 (architecture review), and TT-23 (administrable catalogs) are all moved to `Done`, each with real evidence in its description.
